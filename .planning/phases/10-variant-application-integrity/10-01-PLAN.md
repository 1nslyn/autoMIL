---
phase: "10"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_apl01_iris_dispatch.py
  - tests/test_apl03_loud_fail.py
  - benchmarks/tests/test_variant_dispatch_clam.py
  - benchmarks/tests/test_apl02_real_run.py
  - benchmarks/src/autobench/pipeline/variant_dispatch.py
autonomous: true
requirements:
  - APL-01
  - APL-02
  - APL-03
must_haves:
  truths:
    - "All APL test files exist and are syntactically valid (importable)"
    - "All new tests fail RED with NotImplementedError or ImportError — not syntax errors"
    - "variant_dispatch.py stub is importable with the right function signatures"
    - "Full suite still passes (48 existing tests unaffected)"
  artifacts:
    - path: "tests/test_apl01_iris_dispatch.py"
      provides: "APL-01 iris dispatch + runtime-reaches-worktree tests"
    - path: "tests/test_apl03_loud_fail.py"
      provides: "APL-03 loud-fail + ordering + regression-guard tests"
    - path: "benchmarks/tests/test_variant_dispatch_clam.py"
      provides: "APL-02 arg-threading via stub tests"
    - path: "benchmarks/tests/test_apl02_real_run.py"
      provides: "APL-02 workstation real-run test (pytest.mark.workstation)"
    - path: "benchmarks/src/autobench/pipeline/variant_dispatch.py"
      provides: "APL-02 translation layer stub with correct signatures"
  key_links:
    - from: "tests/test_apl01_iris_dispatch.py"
      to: "examples/sklearn-iris/train.py"
      via: "subprocess or direct call with tmp config.yaml"
      pattern: "classifier_v0|make_classifier"
    - from: "tests/test_apl03_loud_fail.py"
      to: "src/automil/cli/lifecycle/apply.py"
      via: "_classify_variant_route call"
      pattern: "_classify_variant_route|ClickException"
    - from: "benchmarks/tests/test_variant_dispatch_clam.py"
      to: "benchmarks/src/autobench/pipeline/variant_dispatch.py"
      via: "apply_model_variant_to_exp_cfg call"
      pattern: "apply_model_variant_to_exp_cfg|CLAM_ARGS"
---

<objective>
Wave-0 test scaffolding for all three APL requirements. Create RED-failing stubs
for the five new test/source files in VALIDATION.md before any implementation begins.

Purpose: Nyquist compliance — no implementation task runs without its test file
pre-existing. All tests must fail for the right reason (unimplemented function),
not a wrong reason (syntax error or import failure from a missing sibling module).

Output:
  - tests/test_apl01_iris_dispatch.py (RED — APL-01)
  - tests/test_apl03_loud_fail.py (RED — APL-03)
  - benchmarks/tests/test_variant_dispatch_clam.py (RED — APL-02, CI-safe)
  - benchmarks/tests/test_apl02_real_run.py (RED/skip — APL-02, workstation-marked)
  - benchmarks/src/autobench/pipeline/variant_dispatch.py (stub with correct signatures)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-variant-application-integrity/10-RESEARCH.md
@.planning/phases/10-variant-application-integrity/10-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test stubs for APL-01 and APL-03 (framework-side)</name>
  <files>tests/test_apl01_iris_dispatch.py, tests/test_apl03_loud_fail.py</files>
  <read_first>
    - tests/test_lifecycle_apply.py — fixture patterns, adir/config setup, _clear_registry usage
    - src/automil/cli/lifecycle/apply.py:71–144 — _derive_variant_selection, the apply command structure
    - examples/sklearn-iris/train.py:51–76 — the inert bug; reads config.yaml but never dispatches variant
    - examples/sklearn-iris/automil/variants/classifier_v0/logistic_v0.py — make_classifier(seed) signature
    - src/automil/registry/_state.py — LOSS_VARIANTS, MODEL_VARIANTS, _clear_registry
    - src/automil/registry/variants/loss.py — LossVariant ABC and __call__ signature
    - 10-RESEARCH.md §"APL-01 Deep-Dive" + §"APL-03 Deep-Dive" + §"Common Pitfalls"
  </read_first>
  <behavior>
    APL-01 tests (tests/test_apl01_iris_dispatch.py):
    - test_iris_baseline_no_variant: run iris train.py with no model.variant in config; confirm result.json written, composite > 0. PASSES even before fix (regression guard for baseline behavior).
    - test_iris_dispatches_classifier_v0_when_variant_set: write a tmp config.yaml with model.variant=classifier_v0; run iris train.py in a tmp working dir; assert the model object instantiated was from make_classifier (not hardcoded LR). Stub: raise NotImplementedError("APL-01: iris dispatch not yet implemented") in train.py's dispatch branch path — the test should fail RED because the branch does not exist yet (the test verifies the dispatch happened, but iris train.py has no dispatch code). Implementation note: the test can monkeypatch make_classifier in the loaded module and assert it was called.
    - test_iris_applied_variant_reaches_worktree_at_runtime: this is the key test that closes A1. It must prove the variant selection (written by apply) reaches the consumer's train.py in a worktree-like isolated directory, not just that apply wrote config.yaml. Strategy: create a tmp dir simulating a worktree (no config.yaml initially — only files that the overlay would copy), write applied_variant.json (the new file the apply command will create, per the A1 fix in plan 10-02) into the tmp dir, run iris train.py from that tmp dir, assert classifier_v0 was dispatched. This test will fail RED until plans 10-02 (applied_variant.json) and 10-04 (iris dispatch) are both done. Stub the test body with pytest.fail("APL-01 A1: applied_variant.json mechanism not yet implemented") until then.

    APL-03 tests (tests/test_apl03_loud_fail.py):
    - test_loss_variant_raises_click_exception_at_apply_time: register a dummy LossVariant subclass, call _classify_variant_route with a selection dict pointing to it, assert ClickException is raised with message containing "requires loop opening" and "ISSUE-007 / RTA". Stub raises NotImplementedError since _classify_variant_route does not exist yet.
    - test_model_variant_does_not_raise: register a dummy ModelVariant subclass, call _classify_variant_route; assert no exception. Same stub pattern.
    - test_string_selector_loss_does_not_raise: pass a selection with loss.variant="svm" (a string not in LOSS_VARIANTS since it is not a registered LossVariant subclass); assert no exception.
    - test_error_raised_before_config_mutation: monkeypatch _atomic_write_text to record if it was called; register a LossVariant; call apply() (via Click test runner) against a node with a loss variant recipe; assert ClickException raised AND _atomic_write_text was NOT called. This is the ordering integrity test (Pitfall 3).
    - All tests call _clear_registry() in teardown/finally to prevent pollution (Pitfall 2).
  </behavior>
  <action>
    Write tests/test_apl01_iris_dispatch.py with the three test functions above. Use
    pytest tmp_path for directory isolation. For the dispatch test, the approach is:
    write automil/config.yaml with model.variant=classifier_v0 into tmp_path,
    run iris train.py via importlib or subprocess from tmp_path, assert result.json
    exists. The "monkeypatch make_classifier" strategy is simpler than subprocess —
    but only works if train.py is imported (not subprocess'd). Use subprocess with
    a modified PYTHONPATH so the test directory acts as cwd; assert result.json
    composite > 0 AND that the variant dispatch branch ran (use a sentinel file or
    check that the classifier identity matches make_classifier's output, e.g., via
    the same seed producing the same params). For the A1 test, pytest.fail with
    the specific message so it is clearly RED with intent.

    Write tests/test_apl03_loud_fail.py with the four test functions. Import
    _classify_variant_route from src/automil/cli/lifecycle/apply.py inside the test
    (it will fail ImportError until plan 10-02 adds it, satisfying the RED condition).
    Use the _clear_registry fixture pattern from test_lifecycle_apply.py.

    Do NOT implement any production code in this task.
  </action>
  <verify>
    <automated>cd /home/jma/Documents/yinshuol/autoMIL && uv run pytest tests/test_apl01_iris_dispatch.py tests/test_apl03_loud_fail.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    Both files exist; uv run pytest on them shows FAILED or ERROR (not syntax error);
    existing test_lifecycle_apply.py (14 tests) still passes:
    uv run pytest tests/test_lifecycle_apply.py -v shows 14 passed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create variant_dispatch.py stub and APL-02 test stubs (autobench-side)</name>
  <files>
    benchmarks/src/autobench/pipeline/variant_dispatch.py,
    benchmarks/tests/test_variant_dispatch_clam.py,
    benchmarks/tests/test_apl02_real_run.py
  </files>
  <read_first>
    - benchmarks/src/autobench/pipeline/config.py — ModelConfig, ExperimentConfig, TrainConfig dataclass shapes
    - benchmarks/src/autobench/pipeline/clam/train.py:62–92 — _make_clam_args seam; all fields settable via ModelConfig/TrainConfig
    - 10-RESEARCH.md §"APL-02 Deep-Dive" code example for apply_model_variant_to_exp_cfg
    - 10-RESEARCH.md §"Common Pitfalls" — Pitfall 6 (ModelConfig is mutable), Pitfall 7 (entry-path wiring)
    - benchmarks/tests/conftest.py — fixture patterns in benchmarks test suite
    - src/automil/registry/_state.py — MODEL_VARIANTS dict structure, _clear_registry
    - src/automil/registry/spec.py — VariantSpec fields
  </read_first>
  <behavior>
    variant_dispatch.py stub: create benchmarks/src/autobench/pipeline/variant_dispatch.py
    with the correct public function signature:
      apply_model_variant_to_exp_cfg(exp_cfg, automil_dir: Path) -> None
    The stub body raises NotImplementedError("APL-02: not yet implemented").
    Also export __all__ = ["apply_model_variant_to_exp_cfg"].

    test_variant_dispatch_clam.py tests (CI-safe, no GPU, no data):
    - test_apply_variant_patches_model_config: build a minimal ExperimentConfig with
      baseline ModelConfig, register a dummy ModelVariant subclass with
      CLAM_ARGS={"model_size": "big", "dropout": 0.5}, write a tmp automil/config.yaml
      with model.variant=test_variant_v0, call apply_model_variant_to_exp_cfg, assert
      exp_cfg.model.model_size=="big" and exp_cfg.model.dropout==0.5.
    - test_variant_fields_flow_through_make_clam_args: after apply_model_variant_to_exp_cfg,
      call _make_clam_args(exp_cfg, "/tmp") and assert args.model_size=="big" and
      args.drop_out==0.5. This proves the full chain.
    - test_no_variant_is_noop: call apply_model_variant_to_exp_cfg with no model.variant
      in config; assert exp_cfg.model unchanged.
    - test_unknown_variant_raises: set model.variant to a name not in registry;
      assert ValueError raised.
    - test_train_field_override_via_train_config: CLAM_ARGS={"lr": 1e-3}; assert
      exp_cfg.train.lr==1e-3 after apply.
    All tests call _clear_registry() in teardown.

    test_apl02_real_run.py (workstation-only):
    - Mark entire module with pytestmark = pytest.mark.workstation
    - Single test: test_real_clam_run_composite_differs_from_baseline — uses
      AUTOBENCH_CCRCC_ROOT env var, pytest.skip if not set. Body is pytest.fail(
      "APL-02 real-run: not yet implemented — requires CLAM dispatch wired in
      run_experiment.py") until plan 10-03 is done.
  </behavior>
  <action>
    Write benchmarks/src/autobench/pipeline/variant_dispatch.py as the stub.
    Import guard: the file imports yaml, pathlib.Path, and automil.registry.scanner /
    _state / spec at the top level so the import chain is validated from day one.

    Write benchmarks/tests/test_variant_dispatch_clam.py with the five test functions.
    Import _clear_registry from automil.registry._state and call it in a pytest
    fixture autouse teardown. Import apply_model_variant_to_exp_cfg from
    autobench.pipeline.variant_dispatch (will raise NotImplementedError on call,
    satisfying RED).

    Write benchmarks/tests/test_apl02_real_run.py with the single workstation-marked
    test.

    Do NOT implement apply_model_variant_to_exp_cfg logic in this task.
  </action>
  <verify>
    <automated>cd /home/jma/Documents/yinshuol/autoMIL && uv run pytest benchmarks/tests/test_variant_dispatch_clam.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    variant_dispatch.py exists and is importable (no import error).
    test_variant_dispatch_clam.py shows FAILED (NotImplementedError), not syntax error.
    test_apl02_real_run.py exists. Full suite (uv run pytest tests/ -v) still shows
    48 passing tests.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → registry singletons | Tests register dummy variants into module-level dicts; must teardown to prevent state pollution between tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | Registry singletons (MODEL_VARIANTS, LOSS_VARIANTS) | mitigate | Every test that calls @register or scan_variants calls _clear_registry() in teardown; enforced by autouse fixture |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase; all dependencies already in project |
</threat_model>

<verification>
After both tasks:
  uv run pytest tests/test_apl01_iris_dispatch.py tests/test_apl03_loud_fail.py benchmarks/tests/test_variant_dispatch_clam.py -v
  → All new tests FAIL (RED) for the right reasons (NotImplementedError or ImportError on _classify_variant_route)
  uv run pytest tests/ -v
  → 48 pre-existing tests still pass
</verification>

<success_criteria>
1. Five new files exist and are syntactically valid (importable).
2. All new tests are RED (failing for the right reason — missing implementation).
3. No regressions: uv run pytest tests/ -v shows 48 passing.
4. variant_dispatch.py is importable with the correct function signature.
</success_criteria>

<output>
Create .planning/phases/10-variant-application-integrity/10-01-SUMMARY.md when done.
</output>
