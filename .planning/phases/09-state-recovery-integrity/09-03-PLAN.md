---
phase: 09-state-recovery-integrity
plan: "03"
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - src/automil/schemas/result.schema.json
  - src/automil/cells/reconcile.py
autonomous: true
requirements:
  - REC-03

must_haves:
  truths:
    - "result.schema.json enum includes partial as a valid status value"
    - "result.schema.json has an optional termination_reason string property"
    - "_crashed_payload returns status='crash' (not 'crashed') — enum drift fixed"
    - "A payload with status='partial' and termination_reason='sigterm' validates without error"
    - "A payload with status='crashed' fails schema validation (tight enum)"
    - "Existing completed/crash/budget_killed/cancelled payloads still validate"
  artifacts:
    - path: "src/automil/schemas/result.schema.json"
      provides: "Updated enum + termination_reason property"
      contains: "partial"
    - path: "src/automil/cells/reconcile.py"
      provides: "_crashed_payload emits status=crash"
  key_links:
    - from: "src/automil/backends/_orchestrator_daemon.py"
      to: "src/automil/schemas/result.schema.json"
      via: "validate_result() already wired at L1349 — will now accept partial"
---

<objective>
Fix the status vocabulary drift (REC-03, D-05/D-06/D-07):
1. Update result.schema.json to accept "partial" status and optional "termination_reason" field.
2. Fix _crashed_payload in reconcile.py to emit "crash" instead of "crashed".

No schema version bump is needed — the change is purely additive (adds one enum value +
one optional property) and result.schema.json already has "additionalProperties": true.
Document this reasoning in a comment.

Purpose: All framework recovery payloads (partial, timeout, oom, crash paths) must validate
against the schema without normalization errors. This is the prerequisite for Plan 04's
status canonicalization in _collect_or_synthesize_result.

Output: Updated schema file + reconcile.py fix.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-state-recovery-integrity/09-CONTEXT.md
@.planning/phases/09-state-recovery-integrity/09-PATTERNS.md
@.planning/phases/09-state-recovery-integrity/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update result.schema.json — add partial enum value + termination_reason property</name>
  <files>src/automil/schemas/result.schema.json</files>

  <read_first>
    - src/automil/schemas/result.schema.json — full file (L1-24 per RESEARCH.md; confirm current enum and properties)
    - 09-PATTERNS.md §"src/automil/schemas/result.schema.json" — exact JSON replacement for status enum and termination_reason
    - src/automil/schemas/_result.py — validator wiring (confirm Draft202012Validator is the target; no code change needed here)
    - 09-RESEARCH.md §"Area B: Status vocabulary" — backward-compat reasoning and no-version-bump justification
  </read_first>

  <action>
  Make two additive changes to result.schema.json. Do not change any other fields.

  **Change 1 — status enum (per D-05/D-07):**
  In the `properties.status.enum` array, add `"partial"` as the fifth value:
  `["completed", "crash", "budget_killed", "cancelled", "partial"]`
  Keep the existing four values in their current order; append "partial" last.

  **Change 2 — termination_reason property (per D-05/D-07):**
  Add a new optional property `termination_reason` at the same level as `status`:
  ```json
  "termination_reason": {
    "type": "string",
    "description": "Free-form reason for non-completed terminal state. Known values: timeout, oom, sigterm, sigkill, unknown. Not an enum — consumers may extend. Added 2026-06-10 (D-05, REC-03). No schema version bump: change is additive-only; additionalProperties:true already set."
  }
  ```
  termination_reason must NOT be added to `"required"` — it is always optional.

  **No version bump:** Document in a comment inside the description string why no schema
  version field is introduced: "Additive-only change. Existing result.json files with
  status=completed|crash|budget_killed|cancelled continue to validate. Schema versioning
  (for breaking changes) is tracked in DBT-01 / Phase 14."

  After the edit, run a quick sanity check to confirm the file is valid JSON:
  `python -c "import json; json.load(open('src/automil/schemas/result.schema.json'))"` — must
  not raise.
  </action>

  <verify>
    <automated>uv run pytest tests/test_result_schema_validation.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    All existing test_result_schema_validation.py tests GREEN.
    New tests (test_partial_status_validates, test_termination_reason_is_optional,
    test_crashed_drift_value_fails_validation) are now also GREEN — the schema accepts
    partial + termination_reason and rejects "crashed".
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix _crashed_payload in reconcile.py — canonicalize "crashed" → "crash"</name>
  <files>src/automil/cells/reconcile.py</files>

  <read_first>
    - src/automil/cells/reconcile.py L72-92 — _crashed_payload function body (per RESEARCH.md "Current vocabulary audit": _crashed_payload emits "crashed" at L84)
    - 09-PATTERNS.md §"src/automil/backends/_orchestrator_daemon.py" — the _STATUS_CANON pattern showing "crashed" → "crash"
    - 09-RESEARCH.md §"Area B" — table showing "crashed" is NOT in the schema enum (the gap to fix)
  </read_first>

  <action>
  Make one targeted change to reconcile.py: in the `_crashed_payload` function body,
  change the `"status": "crashed"` literal to `"status": "crash"`.

  Also add a module-level constant near the top of reconcile.py (after imports, before
  `aggregate_folds`) that makes the canonicalization rule explicit and machine-greppable:

  ```python
  # D-06 (REC-03): canonical status enum. "crashed" was emitted pre-v1.1 — normalize on write.
  _STATUS_CANON: dict[str, str] = {
      "crashed": "crash",
  }
  ```

  Then in _crashed_payload, set `"status": _STATUS_CANON.get("crashed", "crash")` — or
  simply `"status": "crash"` with a comment `# D-06: canonical value (was "crashed")`.
  Either form is acceptable; the constant is the important artifact for future readers.

  Do NOT change aggregate_folds logic, reconcile_budget_kill, or any other function.
  Do NOT change the `status` values returned by aggregate_folds itself (it already returns
  "partial" for partial folds and "completed" for complete folds — confirmed correct per
  RESEARCH.md; those are already canonical).
  </action>

  <verify>
    <automated>uv run pytest tests/test_crashed_canonicalization.py tests/cells/test_aggregate_folds.py -v 2>&1 | tail -15</automated>
  </verify>
  <done>
    test_crashed_canonicalization.py::test_crashed_payload_returns_crash_not_crashed GREEN.
    test_crashed_canonicalization.py::test_crashed_payload_has_no_crashed_key GREEN.
    Existing tests/cells/test_aggregate_folds.py all GREEN (no regressions to aggregate_folds).
    Full suite: `uv run pytest tests/ -q` shows no new failures beyond pre-existing state.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| archive result.json → schema validator | Untrusted content from the training process is validated; schema is the gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering | result.schema.json — termination_reason is free-form string | accept | termination_reason is a log annotation, not a control-flow value; it is never used as a file path or command argument; free-form is intentional per D-05 |
| T-09-04 | Information Disclosure | "crashed" status drift in reconcile.py | mitigate | D-06 canonicalization ensures exactly one status value reaches storage for crash outcomes; prevents divergence between schema-valid and schema-invalid paths |
| T-09-SC | Tampering | No new packages installed | accept | Pure JSON + Python string change; no pip installs |
</threat_model>

<verification>
```
uv run pytest tests/test_result_schema_validation.py tests/test_crashed_canonicalization.py tests/cells/test_aggregate_folds.py -v
```
All targets GREEN. Schema accepts partial + termination_reason; rejects "crashed".
</verification>

<success_criteria>
- result.schema.json is valid JSON and validates correctly via jsonschema
- status enum = ["completed", "crash", "budget_killed", "cancelled", "partial"]
- termination_reason is an optional string property in the schema
- _crashed_payload emits status="crash" not "crashed"
- All existing schema validation tests remain GREEN
- No regressions in reconcile-related tests (tests/cells/test_reconcile_full.py)
</success_criteria>

<output>
Create `.planning/phases/09-state-recovery-integrity/09-03-SUMMARY.md` when done.
</output>
