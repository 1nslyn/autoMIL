---
phase: 09-state-recovery-integrity
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_sigterm_flush.py
  - tests/test_collect_or_synthesize.py
  - tests/test_handle_timeout.py
  - tests/test_partial_fold_recovery.py
  - tests/test_terminal_writer.py
  - tests/test_terminal_writer_consistency.py
  - tests/test_reconcile_from_archive.py
  - tests/test_aggregate_folds.py
  - tests/test_mil_model_normalization.py
  - tests/cells/test_migrate.py
  - tests/test_result_schema_validation.py
  - tests/test_submit_cell_identity.py
autonomous: true
requirements:
  - REC-01
  - REC-02
  - REC-03
  - REC-04

must_haves:
  truths:
    - "All 10 new Wave-0 test files exist and are importable (pytest collects them without ImportError)"
    - "Every new test function is RED (fails with NotImplementedError or ImportError/AttributeError against current source)"
    - "Two extended test files gain new test functions that also fail RED until implementation lands"
    - "Existing 48-test suite stays GREEN — no regressions introduced by the stubs"
  artifacts:
    - path: "tests/test_sigterm_flush.py"
      provides: "REC-01 flush write-target tests"
    - path: "tests/test_collect_or_synthesize.py"
      provides: "REC-01 fold-first synthesis + REC-03 status canonicalization tests"
    - path: "tests/test_handle_timeout.py"
      provides: "REC-01 D-04 main-PID-first signaling tests"
    - path: "tests/test_partial_fold_recovery.py"
      provides: "REC-01 end-to-end kill simulation + D-01 quarantine tests"
    - path: "tests/test_terminal_writer.py"
      provides: "REC-02 four-artifact write tests"
    - path: "tests/test_terminal_writer_consistency.py"
      provides: "REC-02 rank/TSV agreement tests"
    - path: "tests/test_reconcile_from_archive.py"
      provides: "REC-02 D-11 opt-in refresh tests"
    - path: "tests/test_aggregate_folds.py"
      provides: "REC-03 crashed-canonicalization tests (top-level tests/)"
    - path: "tests/test_mil_model_normalization.py"
      provides: "REC-04 normalization tests"
    - path: "tests/cells/test_migrate.py"
      provides: "REC-04 budget-merge + legacy-cell compat shim tests"
  key_links:
    - from: "tests/cells/test_migrate.py"
      to: "automil.cells.migrate"
      via: "import — RED until Plan 05 ships migrate.py"
    - from: "tests/test_terminal_writer.py"
      to: "automil.terminal_writer"
      via: "import — RED until Plan 06 ships terminal_writer.py"
---

<objective>
Create the Wave-0 test scaffolds for all four REC requirements. Every new test function
must be RED (failing) against the current source tree, verifying that the implementation
tasks in Plans 02–06 are actually needed. Existing 48 tests must stay GREEN.

Purpose: Nyquist rule compliance — no implementation task ships without a pre-existing
failing test. The RED state is the contract that plans 02–06 must satisfy.

Output: 10 new test files + 2 file extensions, all collected by pytest, all new tests RED.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-state-recovery-integrity/09-CONTEXT.md
@.planning/phases/09-state-recovery-integrity/09-VALIDATION.md
@.planning/phases/09-state-recovery-integrity/09-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Wave-0 test stubs for REC-01 and REC-03</name>
  <files>
    tests/test_sigterm_flush.py,
    tests/test_collect_or_synthesize.py,
    tests/test_handle_timeout.py,
    tests/test_partial_fold_recovery.py,
    tests/test_aggregate_folds.py
  </files>

  <read_first>
    - 09-PATTERNS.md §"Test Pattern Assignments" — fixture conventions (_init_git_repo, cli_runner)
    - 09-VALIDATION.md §"Per-Requirement Verification Map" — exact test function names for REC-01/REC-03
    - tests/cells/test_runtime_helpers.py — existing runtime_helpers fixture pattern to mirror
    - tests/cells/test_aggregate_folds.py — note this is tests/cells/ not tests/; new file goes in tests/ (top-level)
  </read_first>

  <action>
  Create five new test files in tests/ (top-level). Each file must be importable and
  collected by pytest. Every test body raises pytest.fail("RED — implementation pending")
  or asserts a condition guaranteed to fail against the current source (preferred: import the
  symbol and call it, letting the actual AttributeError/TypeError prove RED naturally).

  Do NOT use bare `pytest.skip` — the test must be RED (failing), not skipped.

  Convention (copy from tests/cells/conftest.py and tests/test_runner.py:13-34):
  - Module-level docstring citing the REC requirement and decision number
  - `from __future__ import annotations`
  - Standard library imports, then `import pytest`
  - One `_init_git_repo(path)` helper per file if CLI invocation is needed

  File-by-file spec:

  **tests/test_sigterm_flush.py** (REC-01, D-02):
  - `test_sigterm_flush_writes_to_results_dir`: monkeypatch AUTOMIL_RESULTS_DIR to a tmp_path;
    install the SIGTERM handler via `register_sigterm_flush(n=1)`; write one fold result JSON
    in the results_dir; send SIGTERM to current process (os.kill(os.getpid(), signal.SIGTERM));
    assert result.json was written in results_dir (not cwd). RED until D-02 fix ships.
  - `test_sigterm_flush_falls_back_to_cwd_when_env_unset`: when AUTOMIL_RESULTS_DIR is not set,
    flush writes to Path.cwd(). This should be GREEN (existing behavior) — confirm current
    behavior so the fix does not regress it. Mark with a comment: "baseline — must stay GREEN."

  **tests/test_collect_or_synthesize.py** (REC-01 D-03, REC-03):
  - `test_fold_aggregation_tried_before_synthesis`: when archive dir has fold_0_result.json
    but no result.json, `_collect_or_synthesize_result` returns a partial dict (not crash/0.0).
    Use unittest.mock to patch `_orchestrator_daemon` internals needed to call the function
    in isolation. RED until D-03 fold-first insertion ships.
  - `test_oom_synthesis_produces_crash_status_with_reason`: when synthesis path detects OOM
    in log text, result has `status="crash"` and `termination_reason="oom"` (not `status="oom"`).
    RED until D-05/D-06 canonicalization ships.
  - `test_timeout_synthesis_produces_crash_status_with_reason`: same for timeout log signal,
    result has `status="crash"` and `termination_reason="timeout"`. RED until canonicalization.

  **tests/test_handle_timeout.py** (REC-01 D-04):
  Uses `unittest.mock.patch("os.kill")` and `unittest.mock.patch("os.killpg")` — no real
  subprocess needed.
  - `test_sigterm_sent_to_main_pid_first`: call `_handle_timeout` on a mock experiment;
    assert os.kill was called with (pid, SIGTERM) before os.killpg was called. RED until D-04.
  - `test_sigkill_sent_to_process_group_after_grace`: assert os.killpg called with SIGKILL
    after grace window. RED until D-04.
  - `test_grace_seconds_read_from_config`: create daemon with config
    `{"orchestrator": {"timeout_grace_seconds": 3}}`; assert time.sleep called with 3.
    RED until D-04 configurable grace ships.

  **tests/test_partial_fold_recovery.py** (REC-01 end-to-end, D-01 quarantine):
  - `test_kill_with_n_completed_folds_returns_mean_composite`: write 3 of 5 fold result
    JSONs in a tmp archive dir; call `aggregate_folds(archive, 5)`; assert composite equals
    mean of the 3 composites (not 0.0) and status == "partial". Uses existing
    `automil.cells.reconcile.aggregate_folds` (already works — this should be GREEN,
    confirming aggregate_folds already returns "partial" correctly).
  - `test_partial_status_excluded_from_best_node`: create graph with a completed parent and
    a partial child (same composite); call `graph.add_executed(..., status="partial")` — but
    since add_executed does not have status param today, instead call `graph.reconcile` with
    a partial result.json; assert `graph.best_node` is still the completed parent. RED until
    D-01 quarantine guard in graph/terminal_writer ships.
  - `test_partial_status_excluded_from_keep_discard`: a node with `status="partial"` should
    NOT have `"keep"` or `"discard"` assigned as its graph status. RED until D-01 quarantine.

  **tests/test_aggregate_folds.py** (REC-03 D-06 — NOTE: goes in tests/, not tests/cells/
  which already has a test_aggregate_folds.py covering the existing API):
  Name this file `tests/test_crashed_canonicalization.py` to avoid collision with the
  existing `tests/cells/test_aggregate_folds.py`. Adjust the automated verify command
  accordingly (the VALIDATION.md name is aspirational; use the non-colliding name).
  - `test_crashed_payload_returns_crash_not_crashed`: call `_crashed_payload(elapsed_s=100)`;
    assert result["status"] == "crash" (not "crashed"). RED until D-06 fix ships.
  - `test_crashed_payload_has_no_crashed_key`: assert "crashed" not in result["status"].
  </action>

  <verify>
    <automated>uv run pytest tests/test_sigterm_flush.py tests/test_collect_or_synthesize.py tests/test_handle_timeout.py tests/test_partial_fold_recovery.py tests/test_crashed_canonicalization.py --tb=no -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    All five files collected by pytest. Existing suite still green:
    `uv run pytest tests/ -v --ignore=tests/test_sigterm_flush.py
    --ignore=tests/test_collect_or_synthesize.py --ignore=tests/test_handle_timeout.py
    --ignore=tests/test_partial_fold_recovery.py --ignore=tests/test_crashed_canonicalization.py -q`
    shows 0 new failures. New tests are RED (fail, not error-on-import).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Wave-0 test stubs for REC-02 and REC-04, extend two existing files</name>
  <files>
    tests/test_terminal_writer.py,
    tests/test_terminal_writer_consistency.py,
    tests/test_reconcile_from_archive.py,
    tests/test_mil_model_normalization.py,
    tests/cells/test_migrate.py,
    tests/test_result_schema_validation.py,
    tests/test_submit_cell_identity.py
  </files>

  <read_first>
    - 09-PATTERNS.md §"Test Pattern Assignments" — exact class/function names and body sketches for each file
    - tests/test_result_schema_validation.py — existing structure to extend (add after last test function)
    - tests/test_submit_cell_identity.py — existing TestSubmitCellIdentity class to extend with TestMilModelCellIdentity
    - tests/cells/conftest.py — shared fixtures for tests/cells/ subdirectory
  </read_first>

  <action>
  Create five new test files and extend two existing ones. Same RED/GREEN discipline as Task 1.

  **tests/test_terminal_writer.py** (REC-02, D-09/D-10):
  - `test_normal_completion_writes_all_four`: set up a tmp_path with a minimal fake graph
    JSON, completed_dir, archive_dir, results_tsv path; attempt to import and call
    `from automil.terminal_writer import write_terminal_state`; assert ImportError (RED until
    Plan 06). Use `pytest.raises(ImportError)` to capture — this makes the test RED without
    a confusing traceback.
  - `test_cap_kill_writes_all_four`: same import probe for the cap-kill path (same function,
    different result dict with `status="budget_killed"`). RED until Plan 06.
  - `test_graph_updated_before_tsv`: assert that graph.json mtime < results.tsv mtime after
    a write_terminal_state call (fixed write-order invariant). RED until Plan 06.

  **tests/test_terminal_writer_consistency.py** (REC-02):
  - `test_rank_and_tsv_agree_after_terminal_write`: invoke `automil rank` after
    write_terminal_state completes; parse TSV last row and compare composite to
    graph.best_node composite — they must match. RED until Plan 06.

  **tests/test_reconcile_from_archive.py** (REC-02, D-11):
  - `test_from_archive_refreshes_existing_node`: create a graph with an executed node whose
    composite is 0.5; write archive/result.json with composite=0.8; run
    `automil reconcile --from-archive <node_id>`; assert graph composite is now 0.8. RED until
    Plan 06 wires --from-archive.
  - `test_from_archive_skips_running_nodes`: node with status="running" must not be
    overwritten (Pitfall 3 guard). RED until Plan 06.
  - `test_default_reconcile_unchanged`: default `automil reconcile` (no --from-archive) on
    a graph with a live executed node must not change it. GREEN (existing behavior baseline).

  **tests/test_mil_model_normalization.py** (REC-04, D-14):
  - `test_clam_sb_variants_collapse`: `normalize_mil_model("CLAM_SB")` ==
    `normalize_mil_model("clam_sb")` == `normalize_mil_model(" clam sb ")`. RED until Plan 02.
  - `test_normalization_strips_leading_trailing`: `normalize_mil_model("  abmil  ")` == "abmil".
  - `test_normalization_collapses_internal_whitespace`: `normalize_mil_model("clam  sb")` == "clam sb".
  Import: `from automil.cells.state import normalize_mil_model`. RED until Plan 02.

  **tests/cells/test_migrate.py** (REC-04, D-15):
  Mirror the existing tests/cells/ pattern (conftest.py is already there).
  - `test_legacy_cell_loads`: write a cell JSON with `"parent_id": "node_0042"` (no mil_model);
    call `read_cell(path)` — must not raise TypeError; assert `cell.mil_model == "node_0042"`.
    RED until Plan 02 ships the read_cell compat shim.
  - `test_agent_active_merge_sums_consumed`: two agent_active cells for same (dataset, encoder,
    mil_model) → migrate_cells merges them; resulting cell has
    consumed_active_seconds = sum of both. RED until Plan 05.
  - `test_wall_clock_merge_keeps_earliest_started_at`: two wall_clock cells → merged cell
    has started_at = min of both. RED until Plan 05.
  - `test_dry_run_does_not_write`: dry_run=True returns summaries but cell files unchanged.
    RED until Plan 05.

  **Extend tests/test_result_schema_validation.py** (REC-03, D-05/D-07):
  Append three new functions after the last existing test (do not modify existing tests):
  - `test_partial_status_validates`: `validate_result({"composite": 0.42, "status": "partial",
    "termination_reason": "sigterm"})` must not raise. RED until Plan 03 updates schema.
  - `test_termination_reason_is_optional`: `validate_result({"composite": 0.5,
    "termination_reason": "oom"})` — no status field, termination_reason alone is optional.
  - `test_crashed_drift_value_fails_validation`: `with pytest.raises(ValidationError):
    validate_result({"composite": 0.5, "status": "crashed"})` — "crashed" is not in the
    tight enum. This should already be RED (crashed was never in the schema enum); confirm it
    fails validation today, making it a regression guard.

  **Extend tests/test_submit_cell_identity.py** (REC-04, D-12/D-13/D-14):
  Append class TestMilModelCellIdentity after the existing TestSubmitCellIdentity class:
  - `test_explicit_mil_model_flag_keys_cell`: invoke submit with --mil-model clam_sb; read
    the created cell JSON; assert cell["mil_model"] == "clam_sb". RED until Plan 05.
  - `test_reparent_joins_same_cell`: submit node_0001 --mil-model clam_sb → cell A created;
    submit node_0002 --parent node_0001 --mil-model clam_sb → must reuse cell A's cell_id.
    RED until Plans 02+05.
  - `test_missing_mil_model_raises`: submit without --mil-model and no run.mil_model in
    config → result.exit_code != 0 and "required" in result.output. RED until Plan 05.
  - `test_mil_model_normalization_same_cell`: submit twice with "CLAM_SB" and " clam sb " →
    same cell_id produced both times. RED until Plans 02+05.
  </action>

  <verify>
    <automated>uv run pytest tests/test_terminal_writer.py tests/test_terminal_writer_consistency.py tests/test_reconcile_from_archive.py tests/test_mil_model_normalization.py tests/cells/test_migrate.py --tb=no -q 2>&1 | tail -10</automated>
  </verify>
  <done>
    All seven files exist and are collected by pytest. New tests in extended files are RED.
    Full suite (excluding new stubs) stays green:
    `uv run pytest tests/ -q --ignore=tests/test_terminal_writer.py
    --ignore=tests/test_terminal_writer_consistency.py
    --ignore=tests/test_reconcile_from_archive.py
    --ignore=tests/test_mil_model_normalization.py
    --ignore=tests/cells/test_migrate.py -q` shows same pass count as before this plan.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → filesystem | Test stubs write to tmp_path (pytest-managed); no production paths touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-SC | Tampering | No new packages installed in this plan | accept | No npm/pip installs; pure Python stdlib + pytest already present |
</threat_model>

<verification>
After both tasks complete:

```
uv run pytest tests/ -v -q 2>&1 | tail -5
```

Pass count must equal or exceed the baseline 48 tests. No pre-existing test may turn RED.
New test files may show as FAILED (RED is correct) but must not show as ERROR (import errors
mean the stub file itself is broken).
</verification>

<success_criteria>
- 10 new test files exist and are pytest-collectible without import errors
- 2 existing test files have new RED test functions appended
- New functions fail because the symbols they test do not exist yet (ImportError, AttributeError) or because the behavior they assert is not yet implemented
- Pre-existing 48 tests are unchanged and GREEN
- No watch-mode flags, no pytest.skip() on the new functions
</success_criteria>

<output>
Create `.planning/phases/09-state-recovery-integrity/09-01-SUMMARY.md` when done.
</output>
