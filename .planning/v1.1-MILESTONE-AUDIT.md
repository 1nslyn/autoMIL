---
milestone: v1.1
milestone_name: Bug Fixing
audited: 2026-06-12
status: passed
scores:
  requirements: 20/20
  phases: 6/6
  integration: 6/6
  flows: 1/1
  suite: "1058 passed, 0 failed, 53 skipped"
gaps:
  requirements: []   # zero unsatisfied / orphaned
  integration: []    # zero broken seams
  flows: []          # zero broken flows
tech_debt:
  - phase: 10-variant-application-integrity
    items:
      - "APL-02 real-data composite-delta is human/workstation-verified (@pytest.mark.workstation, CI-skipped) — architecturally complete (translation layer + 7 unit tests green); only the real-GPU training-outcome delta is deferred. Same category as v1.0's deferred human sub-gates."
  - phase: nyquist-coverage
    items:
      - "Phases 9/10/11/12 VALIDATION.md frontmatter still reads nyquist_compliant: false (flag never flipped at completion). These phases passed verification with their validation tests green — this is a stale process flag, not a coverage gap. Phases 13/14 are nyquist_compliant: true."
  - phase: 14-housekeeping-tech-debt
    items:
      - "IN-01 (code-review Info): tests/test_framework_purity.py _ALLOWLIST still uses a `:60` line-number anchor key (drift-guarded by test_allowlist_anchors_still_present content matching). clause_07 was switched to stable-text; _ALLOWLIST could follow someday. No correctness risk."
deferred_out_of_scope:
  - "results.tsv schema generalization + viz generic-metric rendering (Phase 8 follow-ups #3/#4) — activate when a third consumer surfaces."
  - "Real SLURM/Ray cluster verification + external hardware shapes — behind requires_slurm/requires_ray/workstation markers."
  - "ISSUE-006 (won't-fix design constraint); RTA/loop-opening cluster (deferred — out of v1.1 scope by design)."
---

# Milestone v1.1 "Bug Fixing" — Audit Report

**Audited:** 2026-06-12
**Status:** ✅ PASSED
**Branch:** `milestone/v1.1-bug-fixing` (ahead of `main` by the full milestone; merge/PR owned by Leo)

## Summary

All 6 phases (9–14) of the v1.1 Bug Fixing milestone are complete, verified, code-reviewed,
and cross-phase integration-checked. All 20 v1.1 requirements are satisfied. The framework test
suite (`tests/`) is **100% green: 1058 passed, 0 failed, 53 skipped**. No critical blockers, no
unsatisfied or orphaned requirements, no broken cross-phase seams.

Scope source: `tasks/test-run-issues.md` defect triage (2026-06-07) + 3 STATE.md deferred items +
the APL variant-application gap. 20 REQ-IDs across 6 categories (REC/APL/CFG/SCH/OPS/DBT).

## Requirements Coverage (20/20 satisfied — 3-source cross-referenced)

| REQ | Phase | Traceability | VERIFICATION | Integration | Final |
|-----|-------|--------------|--------------|-------------|-------|
| REC-01..04 | 9 | Complete | passed (4/4) | WIRED | ✅ satisfied |
| APL-01..03 | 10 | Complete | 3/3 must-haves verified, no automated gaps (human_needed = workstation deferral) | WIRED | ✅ satisfied |
| CFG-01..03 | 11 | Complete | passed | WIRED | ✅ satisfied |
| SCH-01..02 | 12 | Complete | passed | WIRED | ✅ satisfied |
| OPS-01..05 | 13 | Complete | passed (5/5) | WIRED | ✅ satisfied |
| DBT-01..03 | 14 | Complete | passed (3/3 + milestone-close gate) | WIRED | ✅ satisfied |

No orphaned requirements (every REQ-ID appears in its phase's VERIFICATION.md). No `unsatisfied`
requirement → FAIL gate not triggered.

## Phase Verification Roll-Up (6/6)

| Phase | Status | Note |
|-------|--------|------|
| 9 — State & Recovery Integrity | passed | terminal_writer single-writer, partial-fold recovery, cell identity |
| 10 — Variant Application Integrity | passed* | 3/3 must-haves verified, no automated gaps; APL-02 real-data delta = workstation human-check (deferred) |
| 11 — Config & Run Fidelity | passed | None-default plumbing + --override suffix-append |
| 12 — Scheduling & Overlay Isolation | passed | scheduling_policy knob + opt-in editable_overlay_guard |
| 13 — CLI Lifecycle & Operability | passed | cancel direct-kill, dequeue, pending→running, --project, viz.port; code-review converged clean (2 iters) |
| 14 — Housekeeping & Tech Debt | passed | legacy graph migrate-on-read, tick_cells guard, anchor cleanup, clause_11 fix → suite 100% green |

\* Phase 10 VERIFICATION carries `status: human_needed` for the single deferred workstation
composite-delta check; all three APL requirements are code-verified + unit-tested and marked
Complete. Not a blocker.

## Cross-Phase Integration (6/6 seams WIRED)

1. **REC-04 mil_model ↔ APL-02** — WIRED (orthogonal: cell-key budget identity vs applied_variant.json dispatch; no conflict).
2. **REC running-spec metadata → OPS-01 cancel** — WIRED. Daemon writes `metadata.pid/pgid/starttime_ticks` (`_orchestrator_daemon.py:1033-1039`) exactly as `cancel.py:102-105` reads; loud-fail guard when neither opaque_id nor pid+pgid present; confirmed by the real-subprocess `test_cancel_local_direct_kill`.
3. **OPS-03 submit + OPS-01/02 cancel/dequeue total_proposed accounting** — WIRED. add_proposed (+1), mark_running (no-op), cancel/promote (−1 clamped); cancel.py + dequeue.py both route through locked_update → graph.cancel(). No counter drift.
4. **CFG-03 --override + SCH scheduling** — WIRED. GPU selected first (`_find_best_gpu`), override appended to command list second (daemon:982, no shell=True). Disjoint state.
5. **DBT-03 anchor cleanup → D-208 gate + framework-purity** — WIRED. Anchor at `:60`; clause_07 stable-text; `_ALLOWLIST` updated; BCK-04 lint clean (incl. Phase 13's cli/cancel.py allowlist entry).
6. **Framework purity D-206 across all phases** — WIRED. `check_backend_isolation.py src/automil` → OK; zero autobench/benchmarks refs in new Phase 13/14 src files.

## E2E Flow — Node Lifecycle (complete, 0 broken)

propose(--mil-model REC-04) → submit pending→running(OPS-03) → daemon schedules(SCH-01) →
apply variant(APL-01/03) → run with variant(APL-02) + config fidelity(CFG-01/02/03) →
interruptible: cancel(OPS-01)/dequeue(OPS-02)/timeout partial-fold(REC-01) →
terminal state single-sourced(REC-02) + canonical schema(REC-03) → reconcile(REC-04) →
legacy graph round-trips(DBT-01). Every step WIRED; no gap.

## Nyquist Coverage

| Phase | VALIDATION.md | nyquist_compliant | Classification |
|-------|---------------|-------------------|----------------|
| 9 | exists | false | PARTIAL (stale flag — verified passed, tests green) |
| 10 | exists | false | PARTIAL (stale flag) |
| 11 | exists | false | PARTIAL (stale flag) |
| 12 | exists | false | PARTIAL (stale flag) |
| 13 | exists | true | COMPLIANT |
| 14 | exists | true | COMPLIANT |

PARTIAL here means the frontmatter flag was not flipped to true at completion time — NOT that
validation coverage is missing (each phase shipped passing validation tests). Recorded as
process tech-debt, not a coverage gap. Discovery-only; no auto-validate triggered.

## Verdict

**PASSED.** 20/20 requirements satisfied, 6/6 phases verified, 6/6 integration seams wired,
1/1 E2E flow complete, suite 100% green. Accepted deferrals (APL-02 workstation human-check,
9–12 nyquist flags, IN-01 allowlist nit) are documented above and carry no correctness risk.
Ready for `/gsd-complete-milestone v1.1`.
