---
phase: 13-cli-lifecycle-operability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_cli_cancel_resubmit.py
  - tests/test_cli_dequeue.py
  - tests/test_cli.py
  - tests/test_cli_project_option.py
  - tests/test_viz_port_config.py
autonomous: true
requirements:
  - OPS-01
  - OPS-02
  - OPS-03
  - OPS-04
  - OPS-05

must_haves:
  truths:
    - "test_cancel_local_direct_kill xfails with a meaningful message (real subprocess spawned)"
    - "test_cancel_missing_pid_metadata xfails with a meaningful message"
    - "test_cancel_no_starttime_ticks xfails"
    - "test_dequeue_removes_queue_spec, test_dequeue_refuses_running, test_dequeue_pending_no_spec, test_dequeue_unknown_node xfail"
    - "test_submit_existing_pending_marks_running xfails"
    - "test_project_option_project_root, test_project_option_automil_dir, test_project_option_absent_cwd_walk xfail"
    - "test_viz_port_default, test_viz_port_from_config, test_viz_port_explicit_overrides_config xfail"
    - "All existing tests in each file still pass (no regressions from stub additions)"
  artifacts:
    - path: "tests/test_cli_cancel_resubmit.py"
      provides: "OPS-01 RED stubs added to existing file"
      contains: "test_cancel_local_direct_kill"
    - path: "tests/test_cli_dequeue.py"
      provides: "OPS-02 RED stubs — new file"
      contains: "test_dequeue_removes_queue_spec"
    - path: "tests/test_cli.py"
      provides: "OPS-03 RED stub added to existing file"
      contains: "test_submit_existing_pending_marks_running"
    - path: "tests/test_cli_project_option.py"
      provides: "OPS-04 RED stubs — new file"
      contains: "test_project_option_project_root"
    - path: "tests/test_viz_port_config.py"
      provides: "OPS-05 RED stubs — new file"
      contains: "test_viz_port_default"
  key_links:
    - from: "tests/test_cli_cancel_resubmit.py::test_cancel_local_direct_kill"
      to: "subprocess.Popen + os.kill(pid, 0) raises ProcessLookupError"
      via: "real child process — anti-theater constraint from 13-VALIDATION.md"
      pattern: "subprocess.Popen.*sleep"
---

<objective>
Wave-0 Nyquist compliance: write xfail RED test stubs for all 13 OPS behaviors identified
in 13-VALIDATION.md before any implementation touches production code.

Purpose: Establish test scaffolding so each subsequent implementation plan has concrete
failing tests to drive against. The stubs must fail for the RIGHT reason (feature not yet
implemented) and must already encode the full fixture shape the real tests will need.

Output: 5 test files with 13 xfail stubs total (3 new files, 2 files extended). The
OPS-01 stub is the most critical: it MUST spawn a real subprocess (not mock os.killpg)
to satisfy the anti-theater constraint from 13-VALIDATION.md §Critical Anti-Theater
Constraints.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-cli-lifecycle-operability/13-CONTEXT.md
@.planning/phases/13-cli-lifecycle-operability/13-RESEARCH.md
@.planning/phases/13-cli-lifecycle-operability/13-VALIDATION.md
@.planning/phases/13-cli-lifecycle-operability/13-PATTERNS.md
@tests/test_cli_cancel_resubmit.py
@tests/test_cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: OPS-01 RED stubs in test_cli_cancel_resubmit.py</name>
  <files>tests/test_cli_cancel_resubmit.py</files>
  <action>
Add three new xfail test functions to the EXISTING tests/test_cli_cancel_resubmit.py
file (do not modify or delete the existing tests — append only). The existing file already
defines _make_adir, _write_graph, cli_runner, and _isolated_backends fixtures; reuse them.

Also add a `running/local/` subdirectory creation to `_make_adir` if the existing
`_make_adir` only creates `running/` (not `running/local/`). Check the existing helper —
the running spec for local backend lives at `orchestrator/running/local/<node>.json`
per D-169. If `_make_adir` only creates `(orch_dir / "running")`, extend it to also
create `(orch_dir / "running" / "local")` via `parents=True, exist_ok=True` — this avoids
FileNotFoundError in the real implementation tests too.

**test_cancel_local_direct_kill** — `@pytest.mark.xfail(reason="OPS-01 not yet implemented", strict=True)`

Anti-theater constraint (13-VALIDATION.md): this test MUST spawn a real subprocess.
Steps:
1. `_make_adir(tmp_path)` + `monkeypatch.chdir(tmp_path)`.
2. Spawn `proc = subprocess.Popen(["sleep", "60"], start_new_session=True)`.
3. `pgid = os.getpgid(proc.pid)`. Import `_read_proc_starttime` from
   `automil.orchestrator`; call `starttime = _read_proc_starttime(proc.pid)` (may be None
   on non-Linux — that is fine).
4. Write running spec at `adir / "orchestrator" / "running" / "local" / f"{node_id}.json"`:
   `{"id": node_id, "metadata": {"pid": proc.pid, "pgid": pgid}}` — add
   `"starttime_ticks": starttime` to the metadata dict only when `starttime is not None`.
5. Write graph with `_write_graph(adir, {node_id: {"id": node_id, "parent_id": None,
   "type": "proposed", "status": "running", "description": "direct kill test",
   "techniques": [], "metadata": {"backend": "local"}}})`.
6. Invoke `cli_runner.invoke(main, ["cancel", node_id], catch_exceptions=False)`.
7. `time.sleep(0.3)` then `pytest.raises(ProcessLookupError): os.kill(proc.pid, 0)`.
8. Read graph.json back; assert `graph["nodes"][node_id]["status"] == "cancelled"`.

Add a teardown `try/except: proc.kill(); proc.wait()` to clean up in case the test runs
before the feature is implemented and the process is still alive.

**test_cancel_missing_pid_metadata** — `@pytest.mark.xfail(reason="OPS-01 not yet implemented", strict=True)`

1. `_make_adir(tmp_path)` + `monkeypatch.chdir(tmp_path)`.
2. Write running spec at `orchestrator/running/local/<node>.json` containing only
   `{"id": node_id}` — no `opaque_id`, no `metadata` at all.
3. Write graph with `status=running, metadata.backend=local`.
4. Invoke `cli_runner.invoke(main, ["cancel", node_id])` (without catch_exceptions to let
   ClickException propagate through CliRunner).
5. Assert `result.exit_code != 0` and `"corrupted" in result.output.lower() or
   "cannot cancel" in result.output.lower() or "manage" in result.output.lower()`.

**test_cancel_no_starttime_ticks** — `@pytest.mark.xfail(reason="OPS-01 not yet implemented", strict=True)`

Like `test_cancel_local_direct_kill` but omit `starttime_ticks` from the running spec
entirely (simulate non-Linux behavior). The test verifies that cancel succeeds even without
`starttime_ticks` — pid/pgid alone is sufficient. Assert process dead + graph cancelled.

Import additions needed at top of file: `import subprocess`, `import os` (check if already
present; add only what is missing). The `_read_proc_starttime` import is inside the test
body as a lazy import (not at module level) to avoid circular import issues.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cli_cancel_resubmit.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
All existing tests in test_cli_cancel_resubmit.py pass. The three new stubs show as
xfailed (not xpass, not error). `uv run pytest tests/test_cli_cancel_resubmit.py -v`
shows XFAIL for test_cancel_local_direct_kill, test_cancel_missing_pid_metadata,
test_cancel_no_starttime_ticks.
  </done>
</task>

<task type="auto">
  <name>Task 2: OPS-02/03/04/05 RED stubs (3 new files + 1 extension)</name>
  <files>
    tests/test_cli_dequeue.py
    tests/test_cli.py
    tests/test_cli_project_option.py
    tests/test_viz_port_config.py
  </files>
  <action>
Create three new test files and extend one existing file. All new stubs use
`@pytest.mark.xfail(reason="OPS-XX not yet implemented", strict=True)`.

**tests/test_cli_dequeue.py** — NEW FILE (OPS-02, 4 stubs)

Header docstring: "RED stubs for automil dequeue command (OPS-02). Wave-0 Nyquist
compliance — all stubs xfail until 13-03 implements dequeue.py."

Copy the `cli_runner`, `_isolated_backends`, `_make_adir`, `_write_graph` fixture pattern
from test_cli_cancel_resubmit.py (same shape — do not import from the other file, duplicate
locally so the file is self-contained). The `_make_adir` helper here must create
`orchestrator/queue/` and `orchestrator/running/local/` directories.

Four xfail stubs:

`test_dequeue_removes_queue_spec`:
1. `_make_adir(tmp_path)` + `monkeypatch.chdir(tmp_path)`.
2. Write `adir / "orchestrator" / "queue" / f"{node_id}.json"` with
   `{"id": node_id, "spec_version": 1}`.
3. Write graph with `type=proposed, status=pending`.
4. Invoke `cli_runner.invoke(main, ["dequeue", node_id], catch_exceptions=False)`.
5. Assert queue spec file no longer exists.
6. Read graph; assert `graph["nodes"][node_id]["status"] == "cancelled"`.

`test_dequeue_refuses_running`:
1. Write graph with `status=running`.
2. No queue spec needed.
3. Invoke dequeue; assert `exit_code != 0` and output contains "cancel" or "running".

`test_dequeue_pending_no_spec` (idempotent path):
1. Write graph with `type=proposed, status=pending`. No queue spec on disk.
2. Invoke dequeue; assert `exit_code == 0`.
3. Read graph; assert `status == "cancelled"`.

`test_dequeue_unknown_node`:
1. Write empty graph (no nodes).
2. Invoke `dequeue node_9999`; assert `exit_code != 0`.

---

**tests/test_cli.py** — ADD one stub to existing file (OPS-03)

Append `test_submit_existing_pending_marks_running` at the end of the existing file.
`@pytest.mark.xfail(reason="OPS-03 not yet implemented", strict=True)`

This test needs a working `config.yaml` with `run.mil_model` so the submit preflight passes.
Steps:
1. Create a temp dir with `.git/`, `automil/config.yaml` containing:
   ```
   run:
     script: train.py
     mil_model: clam_sb
   ```
   and `automil/orchestrator/queue/` directory.
2. Create a dummy `train.py` in the temp dir.
3. Write `graph.json` with target node `type=proposed, status=pending`.
4. `monkeypatch.chdir(tmp_path)`.
5. Invoke `cli_runner.invoke(main, ["submit", "--node", node_id, "--desc", "test",
   "--files", "train.py", "--mil-model", "clam_sb"], catch_exceptions=False)`.
6. Read graph; assert `graph["nodes"][node_id]["status"] == "running"`.

The `cli_runner` and `_isolated_backends` fixtures already exist in test_cli.py — check
before duplicating. Import `main` from `automil.cli`.

---

**tests/test_cli_project_option.py** — NEW FILE (OPS-04, 3 stubs)

Header docstring: "RED stubs for automil --project PATH group option (OPS-04).
IMPORTANT: every test must reset automil.cli._helpers._PROJECT_OVERRIDE = None in
teardown (use monkeypatch fixture which auto-reverts setattr)."

Imports: `pytest`, `json`, `Path`, `CliRunner` from `click.testing`, `main` from
`automil.cli`. Add an autouse fixture `_reset_project_override` that calls
`monkeypatch.setattr("automil.cli._helpers._PROJECT_OVERRIDE", None)` — wait, the
attribute does not exist yet (pre-implementation). Use `pytest.importorskip` or a
try/except: if `_PROJECT_OVERRIDE` is not yet present on the module, the xfail will fire
naturally when `_find_automil_dir` raises (the feature is absent).

Structure the teardown so it does NOT fail if the attribute is absent. Pattern:
```python
import automil.cli._helpers as _h
if hasattr(_h, "_PROJECT_OVERRIDE"):
    monkeypatch.setattr(_h, "_PROJECT_OVERRIDE", None)
```
Use this in an autouse fixture named `_reset_project_override`.

`test_project_option_project_root`:
1. Create `project_a = tmp_path / "project_a"` with
   `project_a / "automil" / "config.yaml"` containing `run:\n  script: train.py\n`.
2. Create a completely separate cwd directory `tmp_path / "elsewhere"`;
   `monkeypatch.chdir(tmp_path / "elsewhere")`.
3. Invoke `cli_runner.invoke(main, ["--project", str(project_a), "status"],
   catch_exceptions=False)` — status is a safe read-only command.
4. Assert `exit_code == 0` or at least the output does NOT contain
   "No automil/config.yaml found".

`test_project_option_automil_dir`:
1. Like above but pass `--project str(project_a / "automil")` (the automil/ dir directly).
2. Same assertion.

`test_project_option_absent_cwd_walk` (regression):
1. Create `tmp_path / "automil" / "config.yaml"`.
2. `monkeypatch.chdir(tmp_path)`.
3. Invoke `cli_runner.invoke(main, ["status"], catch_exceptions=False)` (no --project).
4. Assert `exit_code == 0` or output does NOT contain "No automil/config.yaml".

---

**tests/test_viz_port_config.py** — NEW FILE (OPS-05, 3 stubs)

Header docstring: "RED stubs for viz port config fallback (OPS-05)."

Use `unittest.mock.patch` or `monkeypatch` to capture the resolved port passed to
`cmd_start`. Since `cmd_start` is imported inside `viz_start`'s function body, patch the
reference at `automil.viz.server.cmd_start`.

Pattern:
```python
from unittest.mock import patch, MagicMock

def test_viz_port_default(tmp_path, monkeypatch, cli_runner):
    # config.yaml with NO viz.port key
    adir = tmp_path / "automil"
    adir.mkdir()
    (adir / "config.yaml").write_text("run:\n  script: train.py\n")
    monkeypatch.chdir(tmp_path)
    captured = {}
    def fake_cmd_start(**kwargs):
        captured.update(kwargs)
    with patch("automil.viz.server.cmd_start", side_effect=fake_cmd_start):
        result = cli_runner.invoke(main, ["viz", "start"], catch_exceptions=False)
    assert captured.get("port") == 8420
```

`test_viz_port_from_config`:
Config contains `viz:\n  port: 9000\n`. Assert `captured["port"] == 9000`.

`test_viz_port_explicit_overrides_config`:
Config contains `viz:\n  port: 9000\n`. Invoke with `["viz", "start", "--port", "7777"]`.
Assert `captured["port"] == 7777`.

Mark all three with `@pytest.mark.xfail(reason="OPS-05 not yet implemented", strict=True)`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cli_dequeue.py tests/test_cli.py tests/test_cli_project_option.py tests/test_viz_port_config.py -x -q 2>&1 | tail -30</automated>
  </verify>
  <done>
All four files parse without import errors. New stubs show as XFAIL. Existing tests in
test_cli.py still pass. Run `uv run pytest tests/ -q` and confirm existing suite stays
green (the 3 new files + extensions contribute only xfail, no unexpected failures).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → subprocess | OPS-01 stub spawns a real child (sleep 60); must clean up in teardown |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan — pure stdlib + existing test patterns |
| T-13-01-TH | Tampering | real subprocess in test | mitigate | teardown try/except proc.kill(); proc.wait() so sleep process cannot outlive test even on xfail |
</threat_model>

<verification>
`uv run pytest tests/test_cli_cancel_resubmit.py tests/test_cli_dequeue.py tests/test_cli.py tests/test_cli_project_option.py tests/test_viz_port_config.py -v 2>&1 | grep -E "PASSED|FAILED|XFAIL|ERROR"`

Expected: 0 FAILED, 0 ERROR, 13 XFAIL (new stubs), all pre-existing tests PASSED.

Full suite regression gate:
`uv run pytest tests/ -q --tb=no 2>&1 | tail -5`
Existing pass count must not decrease.
</verification>

<success_criteria>
- 13 xfail stubs exist across 5 test files (3 new, 2 extended)
- OPS-01 stub spawns a real subprocess.Popen (anti-theater — grep confirms "subprocess.Popen" in test_cli_cancel_resubmit.py)
- OPS-04 stubs include _reset_project_override autouse fixture with safe conditional setattr
- Full framework test suite (tests/ only) stays green
- `uv run pytest tests/ -q --tb=no` shows same pass count as before ± xfail count increase
</success_criteria>

<output>
Create `.planning/phases/13-cli-lifecycle-operability/13-01-SUMMARY.md` when done.
</output>
