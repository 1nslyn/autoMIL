---
phase: 12-scheduling-overlay-isolation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_scheduling_policy.py
  - tests/test_editable_overlay_guard.py
autonomous: true
requirements:
  - SCH-01
  - SCH-02

must_haves:
  truths:
    - "D-01: tests/test_scheduling_policy.py exists with 7 RED stubs for SCH-01 policy dispatch"
    - "D-02: tests/test_editable_overlay_guard.py exists with 5 RED stubs for SCH-02 detect-warn-inject"
    - "Both new test files are importable (no syntax errors); stubs fail with NotImplementedError or xfail, not import errors"
    - "Existing test_orchestrator_env_whitelist.py and test_framework_purity.py remain GREEN (zero regressions)"
  artifacts:
    - path: "tests/test_scheduling_policy.py"
      provides: "7 RED stubs for SCH-01 policy dispatch"
      contains: "test_best_fit_picks_tightest, test_least_loaded_picks_emptiest, test_round_robin_cycles_eligible, test_round_robin_cursor_wraps, test_policy_hot_reload, test_unknown_policy_fallback, test_cursor_not_reset_on_policy_change"
    - path: "tests/test_editable_overlay_guard.py"
      provides: "5 RED stubs for SCH-02 warn + inject"
      contains: "test_check_warns_missing_guard, test_check_no_warn_when_guard_enabled, test_check_no_warn_when_consumer_guard_present, test_opt_in_injection_prepends_pythonpath, test_check_suppresses_when_no_editable_overlap"
  key_links:
    - from: "tests/test_scheduling_policy.py"
      to: "src/automil/backends/_orchestrator_daemon.py"
      via: "_find_best_gpu + scheduling_policy attr"
      pattern: "_find_best_gpu|scheduling_policy|_rr_cursor"
    - from: "tests/test_editable_overlay_guard.py"
      to: "src/automil/cli/check.py + src/automil/backends/_orchestrator_daemon.py"
      via: "check warnings list + _launch PYTHONPATH injection"
      pattern: "editable_overlay_guard|_collect_editable_source_roots"
---

<objective>
Create the Wave-0 RED test stubs for Phase 12. Both new test files must be syntactically valid and importable, and all stubs must fail (not error on import). No production code is written in this plan.

Purpose: Nyquist compliance — tests drive the Wave-2 implementations. The 12-VALIDATION.md §Wave 0 Requirements lists exactly these two files.
Output: tests/test_scheduling_policy.py (7 stubs, SCH-01), tests/test_editable_overlay_guard.py (5 stubs, SCH-02).
</objective>

<execution_context>
@/home/jma/Documents/yinshuol/autoMIL/.planning/phases/12-scheduling-overlay-isolation/12-VALIDATION.md
@/home/jma/Documents/yinshuol/autoMIL/.planning/phases/12-scheduling-overlay-isolation/12-RESEARCH.md
</execution_context>

<context>
@/home/jma/Documents/yinshuol/autoMIL/.planning/PROJECT.md
@/home/jma/Documents/yinshuol/autoMIL/.planning/ROADMAP.md
@/home/jma/Documents/yinshuol/autoMIL/.planning/phases/12-scheduling-overlay-isolation/12-CONTEXT.md
@/home/jma/Documents/yinshuol/autoMIL/tests/test_scheduling_policy.py
@/home/jma/Documents/yinshuol/autoMIL/tests/test_editable_overlay_guard.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write SCH-01 test stubs — test_scheduling_policy.py</name>
  <files>tests/test_scheduling_policy.py</files>
  <read_first>
    - src/automil/backends/_orchestrator_daemon.py:743-766 — _find_best_gpu current logic (candidate tuple shape: (gpu_index, schedulable_free_gb))
    - src/automil/backends/_orchestrator_daemon.py:433-438 — __init__ config-knob read pattern to mirror in stubs
    - src/automil/backends/_orchestrator_daemon.py:1699-1738 — _reload_orchestrator_config hot-reload pattern
    - 12-RESEARCH.md §SCH-01 Code Examples — refactored _find_best_gpu with policy dispatch
  </read_first>
  <action>
Create tests/test_scheduling_policy.py. The file tests the SCH-01 scheduling policy dispatch by constructing a minimal daemon stub — do NOT instantiate OrchestratorDaemon (requires full filesystem); instead build a minimal helper class or call _find_best_gpu as an unbound function against a patched self.

Preferred fixture pattern: define a `FakeDaemon` dataclass/SimpleNamespace with the attrs `_find_best_gpu` uses: `gpu_allocations: dict`, `running: dict`, `max_per_gpu: int`, `safety_margin_gb: float`, `scheduling_policy: str`, `_rr_cursor: int`. Patch `query_gpus` from `automil.backends._orchestrator_daemon` via `unittest.mock.patch`. Import `_find_best_gpu` as an unbound function via `from automil.backends._orchestrator_daemon import OrchestratorDaemon` and call `OrchestratorDaemon._find_best_gpu(fake, needed_gb)`, or extract the function body into a standalone helper for tests — choose whichever import approach avoids needing the full daemon __init__.

Write exactly these 7 test functions, each decorated with `@pytest.mark.xfail(strict=True, reason="SCH-01 not implemented")`:

1. `test_best_fit_picks_tightest` — two GPU candidates with schedulable_free of 4.0 and 8.0 GB; assert best_fit returns the one with 4.0 (index 0), confirming current behavior preserved (per D-01: best_fit = default, preserves current behavior).
2. `test_least_loaded_picks_emptiest` — same two candidates; policy=`least_loaded`; assert returns index of 8.0 GB free GPU.
3. `test_round_robin_cycles_eligible` — three eligible GPU candidates (indices 0, 1, 2); policy=`round_robin`, cursor starts at 0; call twice; assert first call returns index 0, second returns index 1.
4. `test_round_robin_cursor_wraps` — two eligible GPU candidates (indices 0, 1); policy=`round_robin`; call 3 times; assert third call returns index 0 (cursor wraps via modulo).
5. `test_policy_hot_reload` — daemon's `scheduling_policy` attr starts as `best_fit`; simulate _reload_orchestrator_config reading a config dict with `orchestrator.scheduling_policy: round_robin`; assert `self.scheduling_policy` becomes `round_robin` after reload.
6. `test_unknown_policy_fallback` — policy=`turbo_boost` (unknown); assert _find_best_gpu falls back to best_fit behavior (returns tightest-fit GPU, same as test_best_fit).
7. `test_cursor_not_reset_on_policy_change` — set `_rr_cursor = 5`; simulate hot-reload changing policy from `round_robin` to `best_fit` then back to `round_robin`; assert `_rr_cursor` is still 5 (not reset to 0).

All stubs must be marked xfail(strict=True) so they are RED (fail), not ERROR. Do not implement the actual logic — that is Wave 2's job.
  </action>
  <verify>
    <automated>cd /home/jma/Documents/yinshuol/autoMIL && uv run pytest tests/test_scheduling_policy.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>tests/test_scheduling_policy.py exists; `uv run pytest tests/test_scheduling_policy.py -v` reports 7 XFAIL (not ERROR, not PASS). No import errors.</done>
</task>

<task type="auto">
  <name>Task 2: Write SCH-02 test stubs — test_editable_overlay_guard.py</name>
  <files>tests/test_editable_overlay_guard.py</files>
  <read_first>
    - src/automil/cli/check.py:124-171 — check() command structure, warnings list pattern, where editable detection will be inserted (after L164 editable check block)
    - src/automil/backends/_orchestrator_daemon.py:885-895 — _launch call to _build_subprocess_env + where post-processing injection goes (between L894 `env = ...` and L896 `log_path = ...`)
    - 12-RESEARCH.md §SCH-02 — _collect_editable_source_roots() design, check warning template, opt-in injection logic
    - tests/test_orchestrator_env_whitelist.py:135-196 — D-199 invariant tests (PYTHONPATH/AUTOBENCH_ROOT not auto-injected); study the fixture pattern to avoid conflicting with them
  </read_first>
  <action>
Create tests/test_editable_overlay_guard.py. All 5 tests use `@pytest.mark.xfail(strict=True, reason="SCH-02 not implemented")`.

Write exactly these 5 test functions:

1. `test_check_warns_missing_guard` — mock `_collect_editable_source_roots` (from `automil.cli.check`) to return `["/fake/project/src"]`; config has `files.editable: ["src/mymodel.py"]` (overlaps the editable root) and no `orchestrator.editable_overlay_guard`; run script content does NOT contain `sys.path.insert`; invoke the check command logic (or the internal warning-collection helper) and assert the returned warnings list contains a string mentioning "editable" and "worktree" (per D-02 warning shape).

2. `test_check_no_warn_when_guard_enabled` — same setup as test 1 but config has `orchestrator.editable_overlay_guard: true`; assert warnings list does NOT contain any editable-overlay warning.

3. `test_check_no_warn_when_consumer_guard_present` — same editable overlap as test 1 but the run script content contains `sys.path.insert(0, ...)`; assert warnings list does NOT contain editable-overlay warning (consumer self-protection present, per D-03).

4. `test_opt_in_injection_prepends_pythonpath` — construct a fake daemon with `editable_overlay_guard=True`, `project_root=Path("/fake/project")`; mock `_collect_editable_source_roots` to return `["/fake/project/src"]`; create a temp dir acting as `wt_path` with a `src/` subdirectory; call the SCH-02 post-processing injection logic (the block in `_launch` after `_build_subprocess_env`); assert `env["PYTHONPATH"]` starts with `<wt_path>/src`.

5. `test_check_suppresses_when_no_editable_overlap` — `_collect_editable_source_roots` returns `["/other/project/src"]` (no overlap with `files.editable: ["src/mymodel.py"]` under the current project root); assert no editable-overlay warning emitted.

Use `unittest.mock.patch` and `tmp_path` (pytest fixture). Keep imports generic — no `autobench`, `AUTOBENCH_`, or `benchmarks/` references anywhere in the file (framework purity, D-206).
  </action>
  <verify>
    <automated>cd /home/jma/Documents/yinshuol/autoMIL && uv run pytest tests/test_editable_overlay_guard.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>tests/test_editable_overlay_guard.py exists; `uv run pytest tests/test_editable_overlay_guard.py -v` reports 5 XFAIL (not ERROR, not PASS). No import errors. Zero references to autobench/AUTOBENCH_/benchmarks/ in the file.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm D-199 invariant tests still GREEN</name>
  <files></files>
  <action>
Run the existing D-199 invariant tests and the framework purity test to confirm they are still GREEN after adding the two new stub files. No production code was changed in this plan, so these should all pass. If any are RED, investigate before proceeding.

Commands to run:
  uv run pytest tests/test_orchestrator_env_whitelist.py tests/test_framework_purity.py -v

If test_framework_purity.py fails after creating the new stub files (e.g. because a stub file accidentally referenced "benchmarks/" in a comment), fix the stub file to remove the forbidden string.
  </action>
  <verify>
    <automated>cd /home/jma/Documents/yinshuol/autoMIL && uv run pytest tests/test_orchestrator_env_whitelist.py tests/test_framework_purity.py -v 2>&1 | tail -15</automated>
  </verify>
  <done>Both test_orchestrator_env_whitelist.py and test_framework_purity.py pass with zero failures. (These are regression guards for D-199 — zero tolerance.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → production imports | Test stubs import from src/automil/; malformed imports could mask production errors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan (stdlib + existing deps only) |
| T-12-01 | Information Disclosure | test_editable_overlay_guard.py | mitigate | Ensure no autobench/benchmarks/ strings appear in the test file — framework purity gate enforces this at CI |
</threat_model>

<verification>
Full quick-check after Wave 1:
  uv run pytest tests/test_scheduling_policy.py tests/test_editable_overlay_guard.py tests/test_orchestrator_env_whitelist.py tests/test_framework_purity.py -v

Expected: 7 XFAIL + 5 XFAIL + existing passing count. Zero failures, zero errors.
</verification>

<success_criteria>
- tests/test_scheduling_policy.py: 7 xfail stubs, importable, no syntax errors
- tests/test_editable_overlay_guard.py: 5 xfail stubs, importable, no autobench/benchmarks/ refs
- test_orchestrator_env_whitelist.py: still GREEN (D-199 invariant preserved)
- test_framework_purity.py: still GREEN (D-206 purity gate)
</success_criteria>

<output>
Create `.planning/phases/12-scheduling-overlay-isolation/12-01-SUMMARY.md` when done.
</output>
