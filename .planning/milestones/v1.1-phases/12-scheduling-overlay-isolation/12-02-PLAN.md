---
phase: 12-scheduling-overlay-isolation
plan: 02
type: execute
wave: 2
depends_on:
  - "12-01"
files_modified:
  - src/automil/backends/_orchestrator_daemon.py
  - src/automil/templates/config.yaml.j2
autonomous: true
requirements:
  - SCH-01

must_haves:
  truths:
    - "D-01: orchestrator.scheduling_policy config knob exists with values best_fit | round_robin | least_loaded, default best_fit"
    - "D-01: _find_best_gpu dispatches on self.scheduling_policy; best_fit behavior is preserved (no behavior change at default)"
    - "D-01: _rr_cursor is a plain int instance attr initialized to 0 in __init__; NOT reset during _reload_orchestrator_config"
    - "D-01: _reload_orchestrator_config hot-reloads scheduling_policy with logger.info on change (same pattern as max_per_gpu)"
    - "D-01: unknown policy string falls back to best_fit with a logger.warning (no crash)"
    - "SCH-01 test suite passes: all 7 tests GREEN"
    - "test_orchestrator_env_whitelist.py remains GREEN (D-199 invariant untouched)"
    - "test_framework_purity.py remains GREEN (zero autobench/AUTOBENCH_/benchmarks/ refs in new code)"
  artifacts:
    - path: "src/automil/backends/_orchestrator_daemon.py"
      provides: "_find_best_gpu with policy dispatch + _rr_cursor + scheduling_policy in __init__ and _reload_orchestrator_config"
      contains: "scheduling_policy|_rr_cursor|least_loaded|round_robin|SCHEDULING_POLICY"
    - path: "src/automil/templates/config.yaml.j2"
      provides: "scheduling_policy key in orchestrator: section"
      contains: "scheduling_policy"
  key_links:
    - from: "src/automil/backends/_orchestrator_daemon.py:_find_best_gpu"
      to: "self.scheduling_policy"
      via: "if/elif dispatch"
      pattern: "scheduling_policy.*best_fit|round_robin|least_loaded"
    - from: "src/automil/backends/_orchestrator_daemon.py:_reload_orchestrator_config"
      to: "self.scheduling_policy"
      via: "orch_cfg.get(\"scheduling_policy\", self.scheduling_policy)"
      pattern: "_reload_orchestrator_config.*scheduling_policy"
---

<objective>
Implement SCH-01: GPU scheduling-policy knob. Refactor `_find_best_gpu` to dispatch on `self.scheduling_policy` (best_fit / round_robin / least_loaded); add the config knob read in `__init__` and hot-reload in `_reload_orchestrator_config`; add `_rr_cursor` instance attr; add the knob to config.yaml.j2.

Purpose: Operators running compute-bound, low-VRAM jobs can switch to `round_robin` or `least_loaded` so jobs spread across GPUs instead of stacking onto one. Default remains `best_fit` — zero behavior change for existing deployments (per D-01).
Output: Modified _orchestrator_daemon.py + config.yaml.j2; all 7 SCH-01 tests GREEN.
</objective>

<execution_context>
@/home/jma/Documents/yinshuol/autoMIL/.planning/phases/12-scheduling-overlay-isolation/12-VALIDATION.md
@/home/jma/Documents/yinshuol/autoMIL/.planning/phases/12-scheduling-overlay-isolation/12-RESEARCH.md
@/home/jma/Documents/yinshuol/autoMIL/.planning/phases/12-scheduling-overlay-isolation/12-CONTEXT.md
</execution_context>

<context>
@/home/jma/Documents/yinshuol/autoMIL/.planning/PROJECT.md
@/home/jma/Documents/yinshuol/autoMIL/.planning/ROADMAP.md
@/home/jma/Documents/yinshuol/autoMIL/src/automil/backends/_orchestrator_daemon.py
@/home/jma/Documents/yinshuol/autoMIL/src/automil/templates/config.yaml.j2
@/home/jma/Documents/yinshuol/autoMIL/tests/test_scheduling_policy.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement scheduling_policy knob + _find_best_gpu dispatch</name>
  <files>src/automil/backends/_orchestrator_daemon.py</files>
  <read_first>
    - src/automil/backends/_orchestrator_daemon.py:1-30 — module-level constants block (add SCHEDULING_POLICY = "best_fit" here, consistent with existing constant naming)
    - src/automil/backends/_orchestrator_daemon.py:433-438 — __init__ config-knob reads (add self.scheduling_policy and self._rr_cursor here, matching the max_per_gpu read pattern)
    - src/automil/backends/_orchestrator_daemon.py:743-766 — _find_best_gpu current implementation (replace L764-766 with policy dispatch)
    - src/automil/backends/_orchestrator_daemon.py:1699-1738 — _reload_orchestrator_config (add scheduling_policy hot-reload at the end, NOT resetting _rr_cursor)
    - 12-RESEARCH.md §SCH-01 Code Examples — verified refactored _find_best_gpu with all three branches
    - 12-RESEARCH.md §Round-Robin Cursor Design — cycle through eligible candidates in index order
    - 12-RESEARCH.md §Pitfall 1 — do NOT reset _rr_cursor on policy change
    - 12-RESEARCH.md §Pitfall 4 — round_robin must cycle among ELIGIBLE candidates only, not all GPU indices
  </read_first>
  <behavior>
    - test_best_fit_picks_tightest: two GPUs with schedulable 4.0 and 8.0 GB; policy=best_fit → returns GPU with 4.0 GB free (tightest fit, index 0)
    - test_least_loaded_picks_emptiest: same two GPUs; policy=least_loaded → returns GPU with 8.0 GB free (most schedulable)
    - test_round_robin_cycles_eligible: three eligible GPUs indices [0,1,2]; policy=round_robin; call 1→idx 0, call 2→idx 1
    - test_round_robin_cursor_wraps: two eligible GPUs [0,1]; policy=round_robin; calls 1,2,3 → idx 0,1,0
    - test_policy_hot_reload: _reload_orchestrator_config with scheduling_policy=round_robin → self.scheduling_policy becomes "round_robin"
    - test_unknown_policy_fallback: policy="turbo_boost" → falls back to best_fit behavior, logger.warning emitted
    - test_cursor_not_reset_on_policy_change: _rr_cursor=5; hot-reload changes policy; _rr_cursor still 5
  </behavior>
  <action>
Make the following three targeted edits to src/automil/backends/_orchestrator_daemon.py:

EDIT 1 — Module constants (near top of file with other ALL_CAPS constants):
Add the line: `SCHEDULING_POLICY = "best_fit"`

EDIT 2 — __init__ (after the existing orch_cfg block at L433-438):
Add two lines immediately after `self.default_vram = orch_cfg.get(...)`:
  self.scheduling_policy: str = orch_cfg.get("scheduling_policy", SCHEDULING_POLICY)
  self._rr_cursor: int = 0

EDIT 3 — _find_best_gpu (L743-766): Replace the final sort+return block (L764-766) with a three-branch dispatch:

  policy = self.scheduling_policy
  if policy == "least_loaded":
      candidates.sort(key=lambda x: x[1], reverse=True)
      return candidates[0][0]
  elif policy == "round_robin":
      candidates.sort(key=lambda x: x[0])
      chosen = candidates[self._rr_cursor % len(candidates)][0]
      self._rr_cursor += 1
      return chosen
  else:
      if policy != "best_fit":
          logger.warning(
              "Unknown scheduling_policy %r; falling back to best_fit", policy
          )
      candidates.sort(key=lambda x: x[1])
      return candidates[0][0]

EDIT 4 — _reload_orchestrator_config (L1720-1738): Append a hot-reload block for scheduling_policy AFTER the existing safety_margin_gb block. Follow the exact same pattern as max_per_gpu:
  new_policy = orch_cfg.get("scheduling_policy", self.scheduling_policy)
  if new_policy != self.scheduling_policy:
      logger.info(
          "Config reload: scheduling_policy %r -> %r",
          self.scheduling_policy, new_policy,
      )
      self.scheduling_policy = new_policy
  (Do NOT add any reset of self._rr_cursor here — per 12-RESEARCH.md Pitfall 1.)

Verify the function docstring of _find_best_gpu still accurately describes behavior; update it to: "Find a GPU for the pending job according to self.scheduling_policy (best_fit | round_robin | least_loaded)."

No other lines in the file should be modified.
  </action>
  <verify>
    <automated>cd /home/jma/Documents/yinshuol/autoMIL && uv run pytest tests/test_scheduling_policy.py -v 2>&1 | tail -15</automated>
  </verify>
  <done>All 7 tests in test_scheduling_policy.py pass (GREEN, not XFAIL). grep confirms: SCHEDULING_POLICY constant exists; self.scheduling_policy and self._rr_cursor initialized in __init__; _reload_orchestrator_config block includes scheduling_policy hot-reload; no reset of _rr_cursor in reload function.</done>
</task>

<task type="auto">
  <name>Task 2: Add scheduling_policy to config.yaml.j2 + regression check</name>
  <files>src/automil/templates/config.yaml.j2</files>
  <read_first>
    - src/automil/templates/config.yaml.j2:61-67 — orchestrator: section (add scheduling_policy after existing keys)
  </read_first>
  <action>
Add one line to the orchestrator: section of src/automil/templates/config.yaml.j2, after `max_concurrent_per_gpu` and before `default_vram_estimate_gb` (or after the last existing key — keep alphabetical order within the group):

  scheduling_policy: "best_fit"   # best_fit | round_robin | least_loaded

Include a trailing comment so operators understand the options. Do not touch any other part of the template.

Then run the regression suite to confirm no existing tests broke:
  uv run pytest tests/test_orchestrator_env_whitelist.py tests/test_framework_purity.py tests/test_scheduling_policy.py -v

If any test fails, diagnose before committing.
  </action>
  <verify>
    <automated>cd /home/jma/Documents/yinshuol/autoMIL && grep -n "scheduling_policy" /home/jma/Documents/yinshuol/autoMIL/src/automil/templates/config.yaml.j2 && uv run pytest tests/test_scheduling_policy.py tests/test_orchestrator_env_whitelist.py tests/test_framework_purity.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>config.yaml.j2 contains `scheduling_policy: "best_fit"` in the orchestrator: section. All 7 SCH-01 tests GREEN. test_orchestrator_env_whitelist.py GREEN. test_framework_purity.py GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.yaml → daemon | operator-supplied scheduling_policy string enters daemon |
| _find_best_gpu dispatch | policy string controls scheduling branch executed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02 | Tampering | scheduling_policy config value | mitigate | Unknown policy string falls back to best_fit with logger.warning; never eval()'d or used as import path |
| T-12-03 | Denial of Service | _rr_cursor overflow | accept | Python int is arbitrary-precision; monotonic increment never overflows; modulo keeps index in bounds |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed (stdlib + yaml already in use) |
</threat_model>

<verification>
Full SCH-01 gate:
  cd /home/jma/Documents/yinshuol/autoMIL && uv run pytest tests/test_scheduling_policy.py tests/test_orchestrator_env_whitelist.py tests/test_framework_purity.py -v

Purity spot-check:
  grep -rn "autobench\|AUTOBENCH_\|benchmarks/" /home/jma/Documents/yinshuol/autoMIL/src/automil/backends/_orchestrator_daemon.py | grep -v "^Binary\|# D-199\|# CLN\|# DEC\|_ALLOWLIST"

Expected: zero matches outside existing D-199/DEC-01 comments (which are in the pre-existing allowlist).
</verification>

<success_criteria>
- SCHEDULING_POLICY = "best_fit" constant added at module level
- self.scheduling_policy and self._rr_cursor initialized in __init__
- _find_best_gpu docstring updated; dispatch covers best_fit / round_robin / least_loaded / unknown-fallback
- _reload_orchestrator_config hot-reloads scheduling_policy (no _rr_cursor reset)
- config.yaml.j2 orchestrator: section has scheduling_policy key
- All 7 test_scheduling_policy.py tests GREEN
- test_orchestrator_env_whitelist.py GREEN (D-199 invariant)
- test_framework_purity.py GREEN (D-206 purity)
</success_criteria>

<output>
Create `.planning/phases/12-scheduling-overlay-isolation/12-02-SUMMARY.md` when done.
</output>
