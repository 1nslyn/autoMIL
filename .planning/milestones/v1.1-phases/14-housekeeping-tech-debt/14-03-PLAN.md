---
phase: 14-housekeeping-tech-debt
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_tick_cells.py
autonomous: true
requirements:
  - DBT-02
must_haves:
  truths:
    - "The 3 named DBT-02 tick_cells tests pass in isolation AND in the full suite"
    - "Resolving commit 33b5383 is attributed in the test file (a comment cross-references
      DBT-02 / Phase 9 CR-01) so the regression guard is clearly documented"
    - "The 3 tests assert real cells_dir behavior via the ExperimentOrchestrator construction
      path — not a masked path injected externally"
    - "DBT-02 is marked satisfied (checkbox in REQUIREMENTS.md)"
  artifacts:
    - path: "tests/test_tick_cells.py"
      provides: "Attribution comment near the 3 DBT-02 guard tests referencing 33b5383 / Phase 9 CR-01"
      contains: "DBT-02"
  key_links:
    - from: "tests/test_tick_cells.py"
      to: "src/automil/backends/_orchestrator_daemon.py"
      via: "ExperimentOrchestrator.__init__ construction (self.graph wired by 33b5383)"
      pattern: "test_tick_cells_active_to_refusing_new|test_tick_cells_terminating|test_tick_cells_finalized"
---

<objective>
Verify and formally close DBT-02: the 3 pre-existing tick_cells test failures that were
originally Phase-4/6-origin. These tests already pass — Phase 9 CR-01 (commit 33b5383)
incidentally fixed them by wiring self.graph in ExperimentOrchestrator.__init__. This plan
confirms the green status, attributes the fix, adds a light attribution comment to the test
file for maintainability, and marks DBT-02 satisfied in REQUIREMENTS.md.

This is a verify-and-guard plan. Do NOT re-implement anything. Do NOT change test logic.

Purpose: Honest milestone accounting — DBT-02 is satisfied, not deferred. The 3 tests are
the regression guard; the attribution comment makes the phase connection explicit so future
engineers don't wonder why these tests pass without an obvious fix commit.

Output:
- tests/test_tick_cells.py: 1-3 line attribution comment added near the 3 DBT-02 tests
- REQUIREMENTS.md: DBT-02 checkbox marked [x]
- Confirmed: all 13 tick_cells tests pass in isolation and full suite
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/14-housekeeping-tech-debt/14-CONTEXT.md
@.planning/phases/14-housekeeping-tech-debt/14-RESEARCH.md
@tests/test_tick_cells.py
</context>

<read_first>
The 3 DBT-02 guard tests in tests/test_tick_cells.py:
- test_tick_cells_active_to_refusing_new
- test_tick_cells_terminating_fires_cancel_with_cap_reason
- test_tick_cells_finalized_when_running_empty

These test ExperimentOrchestrator.tick() behavior exercising the cells_dir resolution path.
The root cause of the original failures: ExperimentOrchestrator.__init__ never assigned
self.graph. Phase 9 CR-01 (commit 33b5383) added self.graph = ExperimentGraph(...) in
__init__, making the cells_dir path resolve correctly through the production code path.

The RESEARCH.md §DBT-02 section (lines 213-248) contains the full attribution narrative.

REQUIREMENTS.md §Housekeeping & tech debt (DBT):
- [ ] **DBT-02**: The 3 pre-existing tick_cells failures pass...
This checkbox must be flipped to [x] as part of this plan.

Do NOT:
- Add new test functions
- Modify existing test logic
- Change any assertion in the 3 guard tests
- Add imports or fixtures
Only: add an attribution comment block and mark REQUIREMENTS.md.
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Confirm 3 DBT-02 tests pass in isolation and full suite</name>
  <files>tests/test_tick_cells.py</files>
  <action>
    Step 1 — Run the 3 named tests in isolation:
        uv run pytest tests/test_tick_cells.py::test_tick_cells_active_to_refusing_new \
            tests/test_tick_cells.py::test_tick_cells_terminating_fires_cancel_with_cap_reason \
            tests/test_tick_cells.py::test_tick_cells_finalized_when_running_empty -v

    All 3 MUST pass. If any fail, STOP — do not proceed. Report the failure output so
    the root cause can be investigated (this plan does not re-implement anything).

    Step 2 — Run the full tick_cells suite (13 tests) in isolation:
        uv run pytest tests/test_tick_cells.py -v

    All 13 MUST pass.

    Step 3 — Read tests/test_tick_cells.py to find the location of the 3 guard tests
    and any existing comments above them. Identify the best location for the attribution
    comment (a section header comment or a docstring note above the first of the 3 tests).

    Step 4 — Add a brief attribution comment to tests/test_tick_cells.py.
    Find the 3 test functions and add a comment block ABOVE the first one (or as a section
    header comment). The comment should read approximately:

        # DBT-02 regression guard (Phase 9 CR-01, commit 33b5383)
        # ---------------------------------------------------------------
        # These three tests were the "3 pre-existing tick_cells failures" tracked since
        # Phase 4/6 origin. They pass because ExperimentOrchestrator.__init__ now wires
        # self.graph via ExperimentGraph(...), fixing the cells_dir resolution path.
        # The fix was incidental to Phase 9 CR-01 (self.graph assignment). Do NOT remove
        # or weaken these tests — they are the sole regression guard for DBT-02.

    Keep the comment concise (6-8 lines max). Use # comment style (not docstring). Do NOT
    alter any test function body, fixture, or assertion.

    Step 5 — Run the 3 tests again after adding the comment to confirm no breakage:
        uv run pytest tests/test_tick_cells.py::test_tick_cells_active_to_refusing_new \
            tests/test_tick_cells.py::test_tick_cells_terminating_fires_cancel_with_cap_reason \
            tests/test_tick_cells.py::test_tick_cells_finalized_when_running_empty -v

    Step 6 — Mark DBT-02 [x] in .planning/REQUIREMENTS.md.
    Find the line:
        - [ ] **DBT-02**: The 3 pre-existing `tick_cells` failures pass...
    Change [ ] to [x].
  </action>
  <verify>
    <automated>cd /home/jma/Documents/yinshuol/autoMIL && uv run pytest tests/test_tick_cells.py -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    - All 13 tick_cells tests pass (13 passed, 0 failed)
    - Attribution comment present above the 3 DBT-02 guard tests in test_tick_cells.py
    - .planning/REQUIREMENTS.md shows [x] DBT-02
    - No test logic or assertions modified
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tests/test_tick_cells.py → ExperimentOrchestrator | Test-only; no external input, no network, no user data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-01 | Tampering | test_tick_cells.py comment addition | accept | Comment-only change; no test logic altered. The 3 guard tests assert real production behavior — modifying them would weaken the guard, which is why the action explicitly prohibits it |
| T-14-03-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
Isolation check (3 DBT-02 tests):
    uv run pytest tests/test_tick_cells.py::test_tick_cells_active_to_refusing_new \
        tests/test_tick_cells.py::test_tick_cells_terminating_fires_cancel_with_cap_reason \
        tests/test_tick_cells.py::test_tick_cells_finalized_when_running_empty -v

Full tick_cells suite:
    uv run pytest tests/test_tick_cells.py -v

Expected: 13 passed, 0 failed.

REQUIREMENTS.md check:
    grep "DBT-02" .planning/REQUIREMENTS.md
Expected: "- [x] **DBT-02**:..."
</verification>

<success_criteria>
1. All 3 named DBT-02 tests pass in isolation (3 passed, 0 failed)
2. All 13 tick_cells tests pass (full test_tick_cells.py suite)
3. Attribution comment in test_tick_cells.py references "DBT-02", "33b5383", and "Phase 9 CR-01"
4. REQUIREMENTS.md shows [x] for DBT-02
5. No existing test logic changed (diff to test_tick_cells.py is comment-only)
</success_criteria>

<output>
Create .planning/phases/14-housekeeping-tech-debt/14-03-SUMMARY.md when done.
</output>
