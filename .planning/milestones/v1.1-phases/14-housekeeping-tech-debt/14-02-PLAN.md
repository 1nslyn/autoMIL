---
phase: 14-housekeeping-tech-debt
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/automil/backends/_orchestrator_daemon.py
  - tests/test_framework_purity.py
  - tests/acceptance/test_phase8_acceptance.py
autonomous: true
requirements:
  - DBT-03
must_haves:
  truths:
    - "No em-dash (U+2014) characters remain in _orchestrator_daemon.py (the deferral comment
      block at lines 31-36 is trimmed and the em-dash at line 58 is converted to ASCII)"
    - "The deferral marker 'Left here until Phase 14 / DBT-03 anchor cleanup' is removed"
    - "The framework-purity _ALLOWLIST key for _orchestrator_daemon.py is updated to the new
      anchor line number (or switched to stable-text matching) so test_allowlist_anchors_still_present passes"
    - "clause_07 in test_phase8_acceptance.py no longer asserts a hardcoded line number;
      it asserts stable content-anchor text instead"
    - "clause_11 in test_phase8_acceptance.py passes: req_path precedence flipped so
      .planning/milestones/v1.0-REQUIREMENTS.md is read first when it exists (per D-05)"
    - "All other D-208 clauses (1-10, 12+) remain green — the precedence flip is isolated to clause_11"
    - "test_framework_purity.py::test_allowlist_anchors_still_present passes"
    - "Full D-208 acceptance suite passes (0 failed)"
  artifacts:
    - path: "src/automil/backends/_orchestrator_daemon.py"
      provides: "No em-dashes, no deferral comment, clean anchor neighborhood"
      contains: "Consumer-specific vars (e.g. AUTOBENCH_*_ROOT)"
    - path: "tests/test_framework_purity.py"
      provides: "_ALLOWLIST key updated to new anchor line or stable-text match"
      contains: "_orchestrator_daemon.py"
    - path: "tests/acceptance/test_phase8_acceptance.py"
      provides: "clause_07 stable-text anchor + clause_11 precedence fix"
      contains: "Consumer-specific vars (e.g. AUTOBENCH_*_ROOT)"
  key_links:
    - from: "tests/test_framework_purity.py"
      to: "src/automil/backends/_orchestrator_daemon.py"
      via: "_ALLOWLIST content-anchor substring check"
      pattern: "_orchestrator_daemon\\.py"
    - from: "tests/acceptance/test_phase8_acceptance.py"
      to: ".planning/milestones/v1.0-REQUIREMENTS.md"
      via: "clause_11 req_path read (archive-first precedence)"
      pattern: "v1\\.0-REQUIREMENTS\\.md"
---

<objective>
Two atomic sub-tasks in one plan (D-04 + D-05), covering DBT-03 and clause_11.

DBT-03 (D-04): Clean the em-dash characters and the Phase-14 deferral marker from
_orchestrator_daemon.py, then atomically update the framework-purity _ALLOWLIST key
and clause_07's anchor assertion in test_phase8_acceptance.py to reflect the new line
number (or switch to stable-text matching to end the brittleness permanently).

clause_11 (D-05): Flip the req_path precedence in test_phase8_acceptance.py
test_d208_clause_11_state_roadmap_complete so the v1.0 archive is read first when it
exists, making the test pass. This brings the full suite to 0 failed.

Purpose: These two items are the remaining causes of the pre-existing 1 failed / 53 skipped
baseline. Closing them makes the milestone-final suite 100% green.

Output:
- _orchestrator_daemon.py: em-dash(es) replaced with ASCII, deferral comment trimmed
- test_framework_purity.py: _ALLOWLIST entry updated or converted to stable-text
- test_phase8_acceptance.py: clause_07 uses stable-text anchor; clause_11 precedence fixed
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-housekeeping-tech-debt/14-CONTEXT.md
@.planning/phases/14-housekeeping-tech-debt/14-RESEARCH.md
@src/automil/backends/_orchestrator_daemon.py
@tests/test_framework_purity.py
@tests/acceptance/test_phase8_acceptance.py
</context>

<read_first>
Read _orchestrator_daemon.py lines 30-70 before editing to confirm exact current state:
- Lines 31-36: the IN-01 + deferral comment block. The deferral text is at line 36:
  "# allowlist / acceptance anchors. Left here until Phase 14 / DBT-03 anchor cleanup."
- Line 58: "# GITHUB_TOKEN, AWS_SECRET_ACCESS_KEY, ...) are NOT inherited — closing the"
  The `—` (U+2014) is in this line.
- Line 62: "# Consumer-specific vars (e.g. AUTOBENCH_*_ROOT) are opted in per project via"
  This is the content anchor. Do NOT alter this line's content.

Grep before editing to find ALL em-dashes (U+2014, \xe2\x80\x94) in the file:
    grep -n $'—' src/automil/backends/_orchestrator_daemon.py

DBT-03 execution order (CRITICAL — avoids line-miscounting):
1. Convert em-dash(es) in-place first (same line count, just character substitution)
2. Remove/trim the deferral comment lines (causes line shift)
3. Grep for the anchor content "Consumer-specific vars" to confirm its new line number
4. Update _ALLOWLIST in test_framework_purity.py with new line:content pair
5. Update clause_07 in test_phase8_acceptance.py (switch to stable-text)
6. Run tests to confirm all green — then commit

Recommended: switch clause_07 to stable-text instead of updating the hardcoded line.
Current clause_07 at test_phase8_acceptance.py:167:
    assert "src/automil/backends/_orchestrator_daemon.py:62" in test_src
Replace with TWO assertions (both must hold):
    assert "_orchestrator_daemon.py:" in test_src   # file is still tracked in allowlist
    assert "Consumer-specific vars (e.g. AUTOBENCH_*_ROOT)" in test_src  # content anchor

For the _ALLOWLIST in test_framework_purity.py: keep the file:line key format (required by
_is_allowlisted logic) but update the line number to whatever grep confirms after the edit.
The content anchor string "Consumer-specific vars (e.g. AUTOBENCH_*_ROOT)" must remain
identical — that is what the anchor check compares.

clause_11 fix (test_phase8_acceptance.py lines 300-308):
Replace the current logic:
    req_path = _REPO_ROOT / ".planning" / "REQUIREMENTS.md"
    if not req_path.exists():
        req_path = _REPO_ROOT / ".planning" / "milestones" / "v1.0-REQUIREMENTS.md"
    assert req_path.exists(), (
        "Neither .planning/REQUIREMENTS.md nor .planning/milestones/v1.0-REQUIREMENTS.md found"
    )

With the archive-first logic (from RESEARCH.md §Fixed logic):
    archive_path = _REPO_ROOT / ".planning" / "milestones" / "v1.0-REQUIREMENTS.md"
    live_path = _REPO_ROOT / ".planning" / "REQUIREMENTS.md"
    # v1.0 acceptance gate must validate the v1.0 record.
    # Archive takes precedence when it exists (post-v1.0-close state).
    if archive_path.exists():
        req_path = archive_path
    elif live_path.exists():
        req_path = live_path
    else:
        req_path = live_path  # triggers assert below
    assert req_path.exists(), (
        "Neither .planning/milestones/v1.0-REQUIREMENTS.md nor .planning/REQUIREMENTS.md found"
    )

The variable req_path is used after this block at line ~308 (req_text = req_path.read_text()),
so it must remain defined as req_path after the replacement.

IMPORTANT: clause_07 and clause_11 both live in test_phase8_acceptance.py. Edit both
in the same commit as the _orchestrator_daemon.py edit — the four changes (daemon source,
purity allowlist, clause_07, clause_11) are one atomic unit. If the line-number changes
but only half the files are updated, test_allowlist_anchors_still_present fails loudly.
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: DBT-03 — em-dash cleanup in _orchestrator_daemon.py + anchor updates (D-04, per DBT-03)</name>
  <files>src/automil/backends/_orchestrator_daemon.py, tests/test_framework_purity.py, tests/acceptance/test_phase8_acceptance.py</files>
  <action>
    Step 1 — Grep for all em-dashes in the daemon file to get exact locations:
        grep -n $'—' src/automil/backends/_orchestrator_daemon.py

    Step 2 — Read lines 30-70 of _orchestrator_daemon.py to see current state.

    Step 3 — In-place em-dash substitution (NO line count change):
    At line 58 (confirm by grep), the line reads:
        "# GITHUB_TOKEN, AWS_SECRET_ACCESS_KEY, ...) are NOT inherited — closing the"
    Replace the U+2014 em-dash with ` --` (space-hyphen-hyphen) or ` -` to keep ASCII.
    Use the Edit tool. If additional em-dashes are found at other lines in the grep output,
    convert each one in-place using the same ASCII substitution.

    Step 4 — Trim the deferral comment (causes line shift):
    Lines 31-36 currently contain the IN-01 note (lines 31-35) plus the deferral line (36):
        # allowlist / acceptance anchors. Left here until Phase 14 / DBT-03 anchor cleanup.
    Remove ONLY line 36 (the "Left here until Phase 14..." line). Keep lines 31-35
    (the IN-01 note about _collect_editable_source_roots is still accurate and useful).
    Removing one line shifts the anchor from line 62 to line 61.

    Step 5 — Confirm new anchor line number:
        grep -n "Consumer-specific vars" src/automil/backends/_orchestrator_daemon.py
    Record the returned line number (expected: 61 after -1 shift from removing line 36).

    Step 6 — Update _ALLOWLIST in test_framework_purity.py:
    The current key is "src/automil/backends/_orchestrator_daemon.py:62" (line 47).
    Update to the new confirmed line number, e.g.:
        "src/automil/backends/_orchestrator_daemon.py:61":
            "Consumer-specific vars (e.g. AUTOBENCH_*_ROOT)",
    Also update the comment above it (line 46) to note the new shift context.

    Step 7 — Update clause_07 in test_phase8_acceptance.py (line ~167):
    Current:
        assert "src/automil/backends/_orchestrator_daemon.py:62" in test_src
    Replace with stable-text assertions (per RESEARCH.md recommendation — makes DBT-03
    the LAST time this needs touching):
        assert "_orchestrator_daemon.py:" in test_src  # file is tracked in allowlist
        assert "Consumer-specific vars (e.g. AUTOBENCH_*_ROOT)" in test_src  # content anchor

    Run verification after all edits:
        uv run pytest tests/test_framework_purity.py tests/acceptance/test_phase8_acceptance.py::test_d208_clause_07_framework_purity_grep_gate -v
    Both must pass before proceeding to Task 2.
  </action>
  <verify>
    <automated>cd /home/jma/Documents/yinshuol/autoMIL && grep -c $'—' src/automil/backends/_orchestrator_daemon.py && uv run pytest tests/test_framework_purity.py -v -q 2>&1 | tail -10</automated>
  </verify>
  <done>
    - grep for em-dash returns 0 (no U+2014 characters remain in _orchestrator_daemon.py)
    - "Left here until Phase 14 / DBT-03 anchor cleanup" line is gone
    - test_framework_purity.py: all tests pass including test_allowlist_anchors_still_present
    - clause_07 assertion uses stable-text (no hardcoded :62 line number)
    - _ALLOWLIST key matches the new confirmed anchor line number
  </done>
</task>

<task type="auto">
  <name>Task 2: clause_11 — flip req_path precedence to archive-first (D-05)</name>
  <files>tests/acceptance/test_phase8_acceptance.py</files>
  <action>
    Read test_phase8_acceptance.py lines 298-315 to confirm exact current state before editing.

    Replace the req_path resolution block (currently at lines ~302-308) with the archive-first
    logic. The replacement must:
    1. Define archive_path and live_path as separate Path variables
    2. Prefer archive_path when it exists (the v1.0 authoritative record)
    3. Fall back to live_path when archive is absent (mid-v1.0 state, or fresh project)
    4. Keep req_path as the final resolved variable (used at ~line 308: req_path.read_text())
    5. Update the assert message to name the archive first

    Exact replacement (from RESEARCH.md §Fixed logic):

        archive_path = _REPO_ROOT / ".planning" / "milestones" / "v1.0-REQUIREMENTS.md"
        live_path = _REPO_ROOT / ".planning" / "REQUIREMENTS.md"
        # v1.0 acceptance gate must validate the v1.0 record.
        # Archive takes precedence when it exists (post-v1.0-close state).
        if archive_path.exists():
            req_path = archive_path
        elif live_path.exists():
            req_path = live_path
        else:
            req_path = live_path  # triggers assert below
        assert req_path.exists(), (
            "Neither .planning/milestones/v1.0-REQUIREMENTS.md nor .planning/REQUIREMENTS.md found"
        )

    After editing, verify the specific failing test now passes:
        uv run pytest tests/acceptance/test_phase8_acceptance.py::test_d208_clause_11_state_roadmap_complete -v

    Then verify the full D-208 acceptance suite (all clauses):
        uv run pytest tests/acceptance/test_phase8_acceptance.py -v

    All clauses must pass. If any clause OTHER than clause_11 now fails, the edit has
    unintended scope — stop and investigate before committing.
  </action>
  <verify>
    <automated>cd /home/jma/Documents/yinshuol/autoMIL && uv run pytest tests/acceptance/test_phase8_acceptance.py -v -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    - test_d208_clause_11_state_roadmap_complete PASSES (was FAILING before this plan)
    - All other D-208 clauses still pass (no regressions)
    - The full acceptance suite shows 0 failed
    - The full framework suite (uv run pytest tests/ -q) shows 0 failed (milestone-close target)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test_phase8_acceptance.py → filesystem | Reads .planning/milestones/v1.0-REQUIREMENTS.md from the repo root; path is repo-internal, no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-01 | Tampering | _orchestrator_daemon.py em-dash removal | accept | Pure comment/whitespace edit; the security logic (_SPEC_ENV_BLOCKED, subprocess env whitelist) is on different lines and unchanged. Risk: none |
| T-14-02-02 | Tampering | clause_11 precedence flip | accept | The flip reads from .planning/milestones/v1.0-REQUIREMENTS.md (a committed file in the repo). It is a test-only change; no production code path is affected. Other clauses read different files (CHANGELOG.md, source files) and are unaffected |
| T-14-02-03 | Spoofing | clause_11 reading wrong requirements file | mitigate | The archive-first logic is correct semantics for a v1.0 acceptance gate operating post-v1.1; confirmed by direct inspection of both files. The fallback to live_path is safe for pre-close states |
| T-14-02-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
After Task 1:
    uv run pytest tests/test_framework_purity.py tests/acceptance/test_phase8_acceptance.py::test_d208_clause_07_framework_purity_grep_gate -v

After Task 2 (full plan gate):
    uv run pytest tests/ -q

Expected final state:
- 0 em-dash characters in _orchestrator_daemon.py (grep returns 0)
- "Left here until Phase 14" line removed
- test_framework_purity.py: all tests pass
- test_phase8_acceptance.py: all clauses pass (0 failed — was 1 failed for clause_11)
- Full suite: 0 failed (milestone-close gate)
</verification>

<success_criteria>
1. grep -c $'—' src/automil/backends/_orchestrator_daemon.py returns 0
2. "Left here until Phase 14 / DBT-03 anchor cleanup" is absent from _orchestrator_daemon.py
3. test_framework_purity.py::test_allowlist_anchors_still_present passes
4. clause_07 asserts stable content-text (not hardcoded :62 line number)
5. clause_11 (test_d208_clause_11_state_roadmap_complete) passes
6. uv run pytest tests/ -q shows 0 failed (up from 1 failed before Phase 14)
</success_criteria>

<output>
Create .planning/phases/14-housekeeping-tech-debt/14-02-SUMMARY.md when done.
</output>
