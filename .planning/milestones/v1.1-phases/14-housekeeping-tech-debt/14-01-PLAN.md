---
phase: 14-housekeeping-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_graph.py
  - src/automil/graph.py
autonomous: true
requirements:
  - DBT-01
must_haves:
  truths:
    - "A pre-D-200 graph.json (flat val_auc/val_bacc/test_auc/test_bacc, no metrics dict) loads
      through ExperimentGraph without KeyError and yields fully-populated node['metrics'] dicts"
    - "A graph.json with no schema_version key (truly legacy) also migrates correctly"
    - "A post-D-200 graph (schema_version=2, already has metrics dicts) is NOT re-migrated
      (idempotency: existing metrics dicts are untouched, no warning logged for this case)"
    - "The flat top-level metric keys are PRESERVED after migration (keep-flat strategy for back-compat)"
    - "schema_version default in __init__ is bumped to 2 so fresh-init graphs never re-trigger migration"
    - "Migration is in-memory only; __init__ does not call save() or locked_update"
  artifacts:
    - path: "tests/test_graph.py"
      provides: "3 new DBT-01 test functions covering round-trip, absent-version, and idempotency"
      contains: "test_legacy_schema_round_trip"
    - path: "src/automil/graph.py"
      provides: "per-node migrate-on-read block after the top-level/meta defaults block"
      contains: "_LEGACY_METRIC_KEYS"
  key_links:
    - from: "tests/test_graph.py"
      to: "src/automil/graph.py"
      via: "ExperimentGraph(graph_path) — load through the REAL __init__, not a hand-constructed dict"
      pattern: "ExperimentGraph\\(.*graph_path\\)"
    - from: "src/automil/graph.py"
      to: "self._data[\"schema_version\"]"
      via: "gate: self._data.get('schema_version', 1) < 2"
      pattern: "schema_version.*< 2"
---

<objective>
Implement DBT-01: pre-D-200 graph.json files round-trip through ExperimentGraph without
KeyError. Adds a per-node migrate-on-read step in graph.py __init__ that detects the legacy
flat-key schema and spreads the 4 consumer metric keys (val_auc/val_bacc/test_auc/test_bacc)
into a fresh node["metrics"] dict, gated on schema_version < 2 and "metrics" not in node.

Purpose: Any operator who runs autoMIL v1.1 against an old experiment tree written before
the D-200 metrics-dict refactor (Phase 8, commit 10ecb29) gets a clean load instead of a
KeyError or silent mismatch.

Output:
- 3 new tests in tests/test_graph.py (RED first, then GREEN after implementation)
- Migration block in src/automil/graph.py __init__ (after existing top-level/meta defaults block)
- schema_version default bumped 1 → 2 at graph.py:90
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-housekeeping-tech-debt/14-CONTEXT.md
@.planning/phases/14-housekeeping-tech-debt/14-RESEARCH.md
@.planning/phases/14-housekeeping-tech-debt/14-VALIDATION.md
@src/automil/graph.py
@tests/test_graph.py
</context>

<read_first>
Read src/automil/graph.py lines 73-130 before any edit — the __init__ defaults block ends
at ~line 124. The migration block inserts AFTER line 124 (after the existing warning block).

Current schema_version default is at line 90:
    "schema_version": 1,
Bump this to 2.

The full migration algorithm from RESEARCH.md D-02 Resolution section:

    # After the existing top-level/meta defaults block in __init__ (~line 124):
    if self._data.get("schema_version", 1) < 2:
        _LEGACY_METRIC_KEYS = ("val_auc", "val_bacc", "test_auc", "test_bacc")
        for node in self._data.get("nodes", {}).values():
            if "metrics" not in node:
                node["metrics"] = {
                    k: node.get(k, 0.0) for k in _LEGACY_METRIC_KEYS
                }
        self._data["schema_version"] = 2
        if loaded_from_disk:
            logger.warning(
                "graph.json at %s: legacy schema (pre-D-200) detected; "
                "migrated %d node(s) to metrics-dict layout on read. "
                "Re-save to persist the migration.",
                self.path, len(self._data.get("nodes", {})),
            )

IMPORTANT: the migration sets self._data["schema_version"] = 2 INSIDE the if-block so
that already-D-200 graphs (already have schema_version=2, OR graphs loaded with the new
default of 2) skip the block entirely. The default bump (1→2) means fresh-init graphs
and newly-written graphs won't re-trigger on next load.

IMPORTANT: Do NOT add a save() or locked_update() call in __init__. Migration is in-memory
only. The paper-trail warning notifies the operator; they choose when to save.

The 3 test bodies from RESEARCH.md §Validation Architecture are the authoritative fixture
specification — reproduce them exactly. Key anti-theater constraint from VALIDATION.md:
the legacy graph.json fixture MUST be built with flat keys and NO metrics dict, loaded
through ExperimentGraph(graph_path), and the test asserts the MIGRATED result. Do NOT
hand-construct the expected post-migration dict.
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Add DBT-01 RED tests to test_graph.py (before implementation)</name>
  <files>tests/test_graph.py</files>
  <action>
    Append 3 new top-level test functions to tests/test_graph.py (after the last existing test
    function). Do NOT place them inside a class. Each function is a standalone pytest test.

    Function 1 — test_legacy_schema_round_trip(tmp_path):
    Build a legacy graph.json dict with schema_version=1 and one node that has flat metric
    keys (val_auc=0.85, val_bacc=0.80, test_auc=0.87, test_bacc=0.83) but NO "metrics" key.
    Write to tmp_path/graph.json via json.dumps. Load through ExperimentGraph(graph_path).
    Assert: "metrics" in node, node["metrics"]["val_auc"] == 0.85,
    node["metrics"]["val_bacc"] == 0.80, node["metrics"]["test_auc"] == 0.87,
    node["metrics"]["test_bacc"] == 0.83, node["val_auc"] == 0.85 (flat key preserved),
    g._data["schema_version"] == 2.

    Function 2 — test_legacy_schema_absent_version(tmp_path):
    Same structure but omit schema_version from the dict entirely (no key present). Verify
    the migration still fires: "metrics" in node, g._data["schema_version"] == 2.

    Function 3 — test_post_d200_graph_not_remigrated(tmp_path):
    Build a post-D-200 graph.json with schema_version=2 and a node that already has a
    "metrics" dict ({"val_auc": 0.85, "val_bacc": 0.80, "test_auc": 0.87, "test_bacc": 0.83,
    "composite": 0.87}). Load through ExperimentGraph(graph_path). Assert that
    node["metrics"]["val_auc"] == 0.85 (unchanged), g._data["schema_version"] == 2,
    and the metrics dict was NOT modified (no extra keys added by migration).

    Use the exact fixture data from RESEARCH.md §Validation Architecture (14-RESEARCH.md
    lines 504-619). Import json and ExperimentGraph at the top of each function (or use
    the existing import at the top of the test file if already present).

    Run after adding: uv run pytest tests/test_graph.py -k "legacy_schema or post_d200" -v
    All 3 MUST FAIL (RED) at this point — the migration code does not exist yet.
    If any pass, the fixture is invalid (test is broken) — stop and investigate.
  </action>
  <verify>
    <automated>cd /home/jma/Documents/yinshuol/autoMIL && uv run pytest tests/test_graph.py -k "legacy_schema or post_d200" -v 2>&1 | tail -20</automated>
  </verify>
  <done>All 3 new tests collected and FAILING (FAILED not ERROR). Full existing test_graph.py
  suite still passes (26 existing tests unchanged).</done>
</task>

<task type="auto">
  <name>Task 2: Implement DBT-01 migrate-on-read in graph.py</name>
  <files>src/automil/graph.py</files>
  <action>
    Two edits to src/automil/graph.py (read the file first — especially lines 73-130):

    EDIT 1 — Bump schema_version default from 1 to 2 at line ~90:
    In the `defaults` dict inside __init__, change:
        "schema_version": 1,
    to:
        "schema_version": 2,

    EDIT 2 — Add per-node migration block immediately after the existing warning block
    (after the closing of the `if loaded_from_disk and (missing_top or missing_meta):` block,
    which ends at approximately line 124). Insert:

        # DBT-01: migrate pre-D-200 nodes (flat metric keys) to metrics-dict layout on read.
        # Gate: schema_version < 2 AND node lacks "metrics" — idempotent on already-D-200 graphs.
        # Migration is in-memory only; caller decides when to save.
        if self._data.get("schema_version", 1) < 2:
            _LEGACY_METRIC_KEYS = ("val_auc", "val_bacc", "test_auc", "test_bacc")
            _migrated = 0
            for _node in self._data.get("nodes", {}).values():
                if "metrics" not in _node:
                    _node["metrics"] = {
                        k: _node.get(k, 0.0) for k in _LEGACY_METRIC_KEYS
                    }
                    _migrated += 1
            self._data["schema_version"] = 2
            if loaded_from_disk and _migrated > 0:
                logger.warning(
                    "graph.json at %s: legacy schema (pre-D-200) detected; "
                    "migrated %d node(s) to metrics-dict layout on read. "
                    "Re-save to persist the migration.",
                    self.path,
                    _migrated,
                )

    IMPORTANT: the schema_version default bump (Edit 1) ensures fresh-init graphs get
    schema_version=2 in-memory, so they skip the migration gate on first load. Post-D-200
    graphs that were saved with schema_version=2 also skip. Only genuinely legacy graphs
    (schema_version absent or 1 in the file) hit the migration path.

    IMPORTANT: Use _migrated counter so the warning only fires if nodes actually lacked
    "metrics" — this makes idempotency cleaner (a legacy graph where all nodes already
    have metrics from a prior in-memory migration pass logs no warning on reload).

    After editing, run the 3 new tests — all MUST pass (GREEN). Then run the full graph
    test suite to confirm zero regressions.
  </action>
  <verify>
    <automated>cd /home/jma/Documents/yinshuol/autoMIL && uv run pytest tests/test_graph.py -v -q 2>&1 | tail -15</automated>
  </verify>
  <done>All 3 DBT-01 tests pass (GREEN). All pre-existing test_graph.py tests still pass.
  Total test_graph.py count increases by 3 from 26 to 29. No warnings about schema
  migration fire for the post-D-200 idempotency test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| graph.json on disk → ExperimentGraph.__init__ | File loaded from operator-controlled filesystem; no external network or user input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | Tampering | graph.py migrate-on-read | mitigate | Gate strictly on `schema_version < 2` AND `"metrics" not in node` — double-guard prevents accidental re-migration of valid post-D-200 graphs; migration is in-memory only so disk is never corrupted without an explicit save |
| T-14-01-02 | Denial of Service | malformed legacy graph.json | accept | `node.get(k, 0.0)` defaults mean a node with no metric keys at all still produces a valid (zero) metrics dict; no exception path; operator receives a warning log |
| T-14-01-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
Quick check after each task commit:
    uv run pytest tests/test_graph.py -q

Full wave check (run this before closing the plan):
    uv run pytest tests/ -q

Expected after plan closes:
- tests/test_graph.py: 29 passed (26 pre-existing + 3 new DBT-01 tests)
- Full suite: 0 failed (was 1 failed before this phase)
- No regressions in test_integration.py, test_cli.py, test_runner.py
</verification>

<success_criteria>
1. test_legacy_schema_round_trip passes: legacy flat-key graph.json loaded via ExperimentGraph
   yields node["metrics"] with correct 4 consumer keys, flat keys preserved, schema_version=2
2. test_legacy_schema_absent_version passes: absent schema_version triggers the same migration
3. test_post_d200_graph_not_remigrated passes: post-D-200 graph untouched (idempotency)
4. All 26 pre-existing test_graph.py tests still pass
5. uv run pytest tests/ -q shows 0 additional failures beyond the pre-existing clause_11 failure
   (which is addressed in plan 14-02)
</success_criteria>

<output>
Create .planning/phases/14-housekeeping-tech-debt/14-01-SUMMARY.md when done.
</output>
