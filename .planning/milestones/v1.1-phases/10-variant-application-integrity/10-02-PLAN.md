---
phase: "10"
plan: "02"
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - src/automil/cli/lifecycle/apply.py
autonomous: true
requirements:
  - APL-01
  - APL-03
must_haves:
  truths:
    - "D-05: A registered LossVariant callable raises ClickException at apply time with 'requires loop opening — deferred (ISSUE-007 / RTA)' — never silently no-ops"
    - "D-05: The error is raised BEFORE config.yaml is mutated (ordering integrity — Pitfall 3)"
    - "D-01/A1 resolved: apply writes applied_variant.json into archive/<node_id>/ so the orchestrator's existing apply_overlay mechanism copies it into the worktree — the variant selection is reproducible per-experiment state"
    - "AUTOMIL_VARIANT_MODEL env var is injected into spec.env at apply time as runtime fallback for the selection"
    - "String bag_loss selectors and model variants do NOT raise (regression guard)"
    - "All 14 existing test_lifecycle_apply.py tests still pass"
  artifacts:
    - path: "src/automil/cli/lifecycle/apply.py"
      provides: "_classify_variant_route function + applied_variant.json write + spec.env injection"
      contains: "_classify_variant_route"
  key_links:
    - from: "src/automil/cli/lifecycle/apply.py"
      to: "src/automil/registry/_state.py"
      via: "LOSS_VARIANTS lookup after scan_variants"
      pattern: "LOSS_VARIANTS"
    - from: "src/automil/cli/lifecycle/apply.py"
      to: "automil/orchestrator/archive/<node_id>/applied_variant.json"
      via: "json.dumps + _atomic_write_text"
      pattern: "applied_variant.json"
---

<objective>
Two changes to src/automil/cli/lifecycle/apply.py that close APL-03 (loud-fail for
loop-opening variants, D-05) and resolve Assumption A1 (variant selection reaches
the worktree's train.py as reproducible per-experiment state, D-01).

Purpose:
  APL-03: A LossVariant callable cannot be applied through the open _make_clam_args
  seam; it must fail loudly BEFORE any config mutation, not silently no-op.
  A1 fix: config.yaml is gitignored and not in files.editable, so the variant
  selection written there by apply never reaches the worktree. The fix adds a
  second write: applied_variant.json into archive/<node_id>/ which the orchestrator's
  existing apply_overlay mechanism copies into the worktree. An AUTOMIL_VARIANT_MODEL
  env var in spec.env provides a runtime fallback.

Output: modified apply.py with _classify_variant_route + applied_variant.json write.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-variant-application-integrity/10-RESEARCH.md
@.planning/phases/10-variant-application-integrity/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _classify_variant_route to apply.py (APL-03, D-05)</name>
  <files>src/automil/cli/lifecycle/apply.py</files>
  <read_first>
    - src/automil/cli/lifecycle/apply.py:1–144 — FULL current content; understand exact insertion points
    - src/automil/registry/_state.py — LOSS_VARIANTS, POLICY_VARIANTS, _clear_registry; understand dict key structure (is it (parent, name) or name?)
    - src/automil/registry/scanner.py:46–117 — scan_variants, _import_path; understand what populates LOSS_VARIANTS
    - src/automil/registry/variants/loss.py — LossVariant ABC; confirms it is a custom callable
    - 10-RESEARCH.md §"APL-03 Deep-Dive" — classification logic, LOSS_VARIANTS key, code example at lines 510–533
    - 10-RESEARCH.md §"Common Pitfalls" — Pitfall 3 (ordering), Pitfall 5 (string selector vs callable)
    - tests/test_apl03_loud_fail.py — the tests this task must make GREEN
  </read_first>
  <behavior>
    - test_loss_variant_raises_click_exception_at_apply_time: register a dummy LossVariant subclass; call _classify_variant_route with selection containing loss.variant pointing to it; ClickException raised with message containing "requires loop opening" and "ISSUE-007 / RTA". GREEN after this task.
    - test_model_variant_does_not_raise: ModelVariant subclass; _classify_variant_route; no exception. GREEN.
    - test_string_selector_loss_does_not_raise: loss.variant="svm" (not in LOSS_VARIANTS); no exception. GREEN.
    - test_error_raised_before_config_mutation: apply() call with LossVariant node; _atomic_write_text NOT called; ClickException raised. GREEN.
    Note: test_iris_applied_variant_reaches_worktree_at_runtime remains RED (plan 10-04).
  </behavior>
  <action>
    Add _classify_variant_route as a module-level function in apply.py. The function
    signature: _classify_variant_route(selection: dict, variants_root: Path) -> None.

    Classification logic (per D-05 and 10-RESEARCH.md §APL-03):
    1. Check loss.variant: if not None, call scan_variants(variants_root) then check
       LOSS_VARIANTS. The LOSS_VARIANTS key structure must be verified against _state.py
       before implementing — use whatever key format scan_variants populates it with.
       If the loss name resolves to a LossVariant subclass entry in LOSS_VARIANTS,
       raise click.ClickException with: "Loss variant '{name}' is a custom LossVariant
       callable that requires injecting into a closed MIL training loop (ISSUE-007 / RTA).
       It cannot be applied through the open _make_clam_args seam. This variant has NOT
       been applied. Deferred to the RTA milestone."
    2. Check policy.variant symmetrically: if not None, scan_variants then check
       POLICY_VARIANTS. If present (registered PolicyVariant subclass that cannot be
       expressed as a string args.opt selector), raise with equivalent message substituting
       "Policy variant" and "ISSUE-007 / RTA".
    3. Model variants: no raise (they apply through CLAM_ARGS on ModelConfig — seam-expressible).
    4. String bag_loss selectors in model CLAM_ARGS: no raise (not in LOSS_VARIANTS).

    Insert the _classify_variant_route call in apply() AFTER selection is derived
    and the empty-selection check, but BEFORE the raw_yaml load and BEFORE shutil.copy2
    (backup) and BEFORE _atomic_write_text. Current apply.py line order:
      line 101: selection = _derive_variant_selection(node)
      line 103–113: empty-selection hard-fail
      line 115: raw_yaml = yaml.safe_load(...)  ← insert BEFORE this
      line 133–136: backup + write
    Insert: variants_root = adir / "variants"; _classify_variant_route(selection, variants_root)
    This ensures classification fires before ANY config mutation (D-05, Pitfall 3).

    Import additions at top of file: scan_variants (lazy import inside function is
    also acceptable to avoid circular imports — match existing import style in apply.py).

    Validate variant_name is a safe path component before constructing variants_root path:
    if "/" in variant_name or ".." in variant_name: raise ClickException("Invalid variant
    name"). Apply this in both _classify_variant_route and the applied_variant write below.
    (Security: V5 input validation per 10-RESEARCH.md §Security Domain.)
  </action>
  <verify>
    <automated>cd /home/jma/Documents/yinshuol/autoMIL && uv run pytest tests/test_apl03_loud_fail.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    uv run pytest tests/test_apl03_loud_fail.py -v shows all 4 tests PASSED.
    uv run pytest tests/test_lifecycle_apply.py -v shows 14 PASSED (no regressions).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write applied_variant.json into overlay archive (A1 fix, D-01)</name>
  <files>src/automil/cli/lifecycle/apply.py</files>
  <read_first>
    - src/automil/cli/lifecycle/apply.py — current state after Task 1
    - src/automil/cli/submit.py:414–443 — spec structure; overlay_dir is "archive/{node}"; queue_file is automil/orchestrator/queue/{node}.json; spec.env is layer 4 in _build_subprocess_env
    - src/automil/backends/_orchestrator_daemon.py:878–883 — apply_overlay reads spec.overlay_dir = "archive/{node_id}" from orch_dir; so applied_variant.json must be at automil/orchestrator/archive/{node_id}/applied_variant.json
    - src/automil/runner.py:54–94 — apply_overlay copies all non-metadata files from overlay_dir into worktree; applied_variant.json would be copied to <worktree>/applied_variant.json
    - 10-RESEARCH.md §"Open Questions #2" — full A1 analysis
    - tests/test_apl01_iris_dispatch.py — test_iris_applied_variant_reaches_worktree_at_runtime stub
  </read_first>
  <behavior>
    test_iris_applied_variant_reaches_worktree_at_runtime: The test simulates a worktree
    dir (no config.yaml, only applied_variant.json written by apply). After plan 10-04
    wires iris train.py to read applied_variant.json, this test goes GREEN. For now it
    remains RED (pytest.fail stub). This task only produces the file; the consumer-side
    read is plan 10-04.

    The test for the write mechanism itself can be added to test_lifecycle_apply.py or
    kept inline in test_apl01_iris_dispatch.py as a unit test:
    - test_apply_writes_applied_variant_json: call apply() against a node with a model
      variant; assert automil/orchestrator/archive/{node_id}/applied_variant.json exists
      and contains {"model": {"variant": "...", "parent": "..."}, "loss": {...}, "policy": {...}}.
    MANDATORY — add to tests/test_lifecycle_apply.py:
    - test_apply_writes_applied_variant_json: call apply() against a node with a model
      variant; assert archive/<node_id>/applied_variant.json EXISTS AND its content
      matches the selection dict with correct key names:
      {"model": {"variant": "<name>", "parent": ...}, "loss": {...}, "policy": {...}}.
      This is the A1 write-mechanism proof and MUST be GREEN after this task.

    MANDATORY — add to tests/test_lifecycle_apply.py or tests/test_apl01_iris_dispatch.py:
    - test_apply_injects_env_var_into_queue_spec: set up a node with a model variant
      AND a pre-existing queue spec at automil/orchestrator/queue/{node_id}.json;
      call apply(); read back the queue spec file; assert
      spec["env"]["AUTOMIL_VARIANT_MODEL"] equals the variant name string.
      This closes success criterion #4 (env injection tested, not merely described)
      and MUST be GREEN after this task.
  </behavior>
  <action>
    After the _classify_variant_route call (Task 1) and AFTER the config.yaml write,
    add a second write that persists the selection as applied_variant.json into the
    experiment's overlay archive directory.

    Exact logic to add at the end of apply() after _atomic_write_text:

    1. Derive the node_id: it is already in scope as node_id (the CLI argument).
    2. Compute archive_dir: adir / "orchestrator" / "archive" / node_id
    3. Create archive_dir with mkdir(parents=True, exist_ok=True).
    4. Write applied_variant.json: json.dumps(selection, indent=2) to
       archive_dir / "applied_variant.json" using _atomic_write_text (not bare open()).
    5. Inject into spec.env for runtime fallback: read the queue spec if it exists at
       adir / "orchestrator" / "queue" / f"{node_id}.json"; if present, update
       spec["env"] = spec.get("env", {}); spec["env"]["AUTOMIL_VARIANT_MODEL"] =
       selection["model"].get("variant") or ""; re-write the queue spec atomically.
       This env var is the runtime fallback for consumers that prefer env vars over
       file reads.
    6. Emit an additional echo: "Wrote applied_variant.json to archive/{node_id}/
       (will be overlaid into worktree by orchestrator)."

    Import json at the top of apply.py (check if already present; add if not).

    Variant name path-safety validation (from Task 1) applies here too: the selection
    values are already validated before this code runs (classify fires first).

    The applied_variant.json format is:
    {
      "model": {"variant": "classifier_v0", "parent": null},
      "loss": {"variant": null},
      "policy": {"variant": null}
    }
    This is exactly the selection dict returned by _derive_variant_selection — write it
    directly without transformation.
  </action>
  <verify>
    <automated>cd /home/jma/Documents/yinshuol/autoMIL && uv run pytest tests/test_lifecycle_apply.py tests/test_apl03_loud_fail.py -v 2>&1 | tail -30</automated>
  </verify>
  <done>
    uv run pytest tests/test_lifecycle_apply.py shows all pre-existing tests PASSED plus
    BOTH of the following MANDATORY new tests GREEN:
    - test_apply_writes_applied_variant_json PASSED: archive/<node_id>/applied_variant.json
      exists AND content matches selection dict with correct key names.
    - test_apply_injects_env_var_into_queue_spec PASSED: spec["env"]["AUTOMIL_VARIANT_MODEL"]
      equals the variant name string after apply().
    uv run pytest tests/test_apl03_loud_fail.py shows 4 passed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.yaml → apply.py | variant name strings read from config/node; used in Path construction and registry lookup |
| apply.py → archive dir write | applied_variant.json written to automil/orchestrator/archive/{node_id}/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | Tampering | variant_name path construction in _classify_variant_route | mitigate | Validate variant_name contains no "/" or ".." using Path(name).name == name check before constructing variants_root / variant_name |
| T-10-03 | Tampering | applied_variant.json written to archive dir | mitigate | archive_dir path is derived from adir (validated by _find_automil_dir) + "orchestrator/archive/" + node_id; node_id is from graph (trusted); no user-controlled path component |
| T-10-04 | Tampering | Loop-opening variant bypasses classification guard | mitigate | _classify_variant_route fires BEFORE any config mutation; confirmed in apply() call order; covered by test_error_raised_before_config_mutation |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
uv run pytest tests/test_lifecycle_apply.py tests/test_apl03_loud_fail.py -v
  → 14 + 4 = 18 tests passing
uv run pytest tests/ -v
  → all pre-existing tests pass, no regressions
grep -n "_classify_variant_route\|applied_variant.json" src/automil/cli/lifecycle/apply.py
  → both symbols present
</verification>

<success_criteria>
1. APL-03: LossVariant raises ClickException with correct message (4 tests GREEN).
2. APL-03: Error fires BEFORE config.yaml is mutated (ordering test GREEN).
3. A1 fix: applied_variant.json written to archive/{node_id}/ after apply (test GREEN).
4. A1 fix: AUTOMIL_VARIANT_MODEL injected into spec.env when queue spec exists.
5. Zero regressions in test_lifecycle_apply.py (14 tests).
</success_criteria>

<output>
Create .planning/phases/10-variant-application-integrity/10-02-SUMMARY.md when done.
</output>
