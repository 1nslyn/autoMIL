---
phase: "10"
plan: "04"
type: execute
wave: 3
depends_on:
  - "10-02"
  - "10-03"
files_modified:
  - examples/sklearn-iris/train.py
autonomous: false
requirements:
  - APL-01
  - APL-02
must_haves:
  truths:
    - "D-01: iris train.py reads applied_variant.json (or AUTOMIL_VARIANT_MODEL env fallback) and imports + calls make_classifier(seed) — not the hardcoded LogisticRegression baseline"
    - "D-02: the applied run instantiates a different model object (classifier_v0.make_classifier return) than the un-applied baseline — assertable in CI with no external data"
    - "No automil.* imports in iris train.py (consumer-decoupled contract, Pitfall 4)"
    - "Baseline still works when no variant is set (backward compatible)"
    - "test_iris_dispatches_classifier_v0_when_variant_set GREEN"
    - "test_iris_applied_variant_reaches_worktree_at_runtime GREEN — closes the A1 inert-variant trap"
    - "test_iris_baseline_no_variant GREEN (regression guard)"
  artifacts:
    - path: "examples/sklearn-iris/train.py"
      provides: "variant dispatch using importlib (no automil.* imports)"
      contains: "applied_variant.json"
  key_links:
    - from: "examples/sklearn-iris/train.py"
      to: "examples/sklearn-iris/automil/variants/classifier_v0/logistic_v0.py"
      via: "importlib.util.spec_from_file_location"
      pattern: "make_classifier|spec_from_file_location"
    - from: "examples/sklearn-iris/train.py"
      to: "applied_variant.json"
      via: "json.loads read from worktree automil/ directory"
      pattern: "applied_variant.json"
---

<objective>
Fix the inert-variant bug in the sklearn-iris reference consumer. After `automil apply`,
the variant selection is now available in applied_variant.json inside the worktree's
automil/ directory (written by plan 10-02). This plan makes iris train.py read it and
dispatch to the registered variant's make_classifier instead of hard-coding
LogisticRegression. No automil.* imports (consumer-decoupled contract preserved,
Pitfall 4 from 10-RESEARCH.md).

Purpose: D-01/D-02 — the APL-01 requirement. The A1 inert-variant trap is closed:
the variant reaches the worktree via the overlay mechanism (plan 10-02), and this plan
makes the consumer actually read and dispatch it.

Output: modified examples/sklearn-iris/train.py with applied_variant.json dispatch.
All three APL-01 tests in test_apl01_iris_dispatch.py GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-variant-application-integrity/10-RESEARCH.md
@.planning/phases/10-variant-application-integrity/10-02-SUMMARY.md
@.planning/phases/10-variant-application-integrity/10-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add applied_variant.json dispatch to iris train.py (APL-01, D-01/D-02)</name>
  <files>examples/sklearn-iris/train.py</files>
  <read_first>
    - examples/sklearn-iris/train.py:1–81 — FULL current content; understand exact structure before editing
    - examples/sklearn-iris/automil/variants/classifier_v0/logistic_v0.py — make_classifier(seed) signature and return type
    - examples/sklearn-iris/automil/variants/classifier_v0/__init__.py — confirm no auto-imports
    - 10-RESEARCH.md §"APL-01 Deep-Dive" code examples (lines 421–441) — the exact importlib dispatch pattern
    - 10-RESEARCH.md §"Common Pitfalls" — Pitfall 4 (no automil.* imports), Pitfall 1 (sys.path not needed for direct importlib load)
    - tests/test_apl01_iris_dispatch.py — the three tests that must go GREEN; read their exact assertions to understand what to prove
    - src/automil/cli/lifecycle/apply.py — applied_variant.json format: {"model":{"variant":"...","parent":...},"loss":{...},"policy":{...}}
  </read_first>
  <behavior>
    - test_iris_baseline_no_variant: no applied_variant.json, no AUTOMIL_VARIANT_MODEL env var; train.py runs; result.json written; composite > 0; model is LogisticRegression. GREEN (was already passing; must stay GREEN).
    - test_iris_dispatches_classifier_v0_when_variant_set: applied_variant.json with model.variant=classifier_v0 present in cwd/automil/; train.py runs; make_classifier is called (monkeypatched or confirmed by model identity); result.json written. GREEN after this task.
    - test_iris_applied_variant_reaches_worktree_at_runtime: simulates worktree run — no config.yaml, only applied_variant.json in cwd/automil/; train.py runs; variant dispatched correctly; result.json written. GREEN after this task. This test is the definitive A1 closure.
  </behavior>
  <action>
    Edit examples/sklearn-iris/train.py in the main() function, between the config read
    block and the clf = LogisticRegression(...) line. Insert the dispatch block using
    the code pattern from 10-RESEARCH.md §"APL-01 Deep-Dive" (lines 421–441):

    Read strategy order (per critical_design_directive — reproducible per-experiment state):
    1. First: try reading config_path.parent / "applied_variant.json" (the overlay file
       written by apply.py and copied into the worktree by apply_overlay).
    2. Fallback: try reading model.variant from config.yaml (existing config read block).
    3. Last fallback: read os.environ.get("AUTOMIL_VARIANT_MODEL") (runtime env var).

    The dispatch block (using raw importlib — NO automil.* imports, Pitfall 4):
      applied_path = Path("automil/applied_variant.json")
      variant_name = None
      if applied_path.exists():
          import json as _json
          _sel = _json.loads(applied_path.read_text()) or {}
          variant_name = (_sel.get("model") or {}).get("variant")
      if not variant_name:
          variant_name = (config.get("model") or {}).get("variant")
      if not variant_name:
          variant_name = os.environ.get("AUTOMIL_VARIANT_MODEL")

      clf = None
      if variant_name:
          # Validate: no path traversal
          import pathlib as _pl
          if _pl.Path(variant_name).name != variant_name:
              raise ValueError(f"Invalid variant name: {variant_name!r}")
          import importlib.util as _ilu
          _variants_dir = Path("automil/variants") / variant_name
          _py_files = sorted(
              f for f in _variants_dir.glob("*.py")
              if not f.name.startswith("_") and f.name != "__init__.py"
          )
          if _py_files:
              _spec = _ilu.spec_from_file_location(variant_name, _py_files[0])
              _mod = _ilu.module_from_spec(_spec)
              _spec.loader.exec_module(_mod)
              clf = _mod.make_classifier(seed=seed)
      if clf is None:
          clf = LogisticRegression(max_iter=200, random_state=seed)
      clf.fit(X_train, y_train)

    Remove the existing bare:
      clf = LogisticRegression(max_iter=200, random_state=seed).fit(X_train, y_train)
    line and replace with the dispatch block above + clf.fit(X_train, y_train).

    Note: json is already imported at the top (or add it if not present). pathlib.Path
    is already imported. importlib.util is stdlib — no new dependencies.

    The config.yaml read block at lines 59-63 already reads data.seed. Add the
    applied_variant.json read AFTER this block using the same config_path.parent base
    so it resolves relative to the same automil/ directory.

    Do NOT add any from automil import ... line. The consumer-decoupled contract
    (train.py:8 comment) must remain intact. The dispatch is purely stdlib.
  </action>
  <verify>
    <automated>cd /home/jma/Documents/yinshuol/autoMIL && uv run pytest tests/test_apl01_iris_dispatch.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    uv run pytest tests/test_apl01_iris_dispatch.py -v shows 3 PASSED.
    Crucially: test_iris_applied_variant_reaches_worktree_at_runtime PASSED — this is
    the definitive A1 closure (variant reaches consumer train.py at runtime, not merely
    written to config).
    uv run pytest tests/ benchmarks/tests/ -v -k "not workstation" passes fully (no regressions).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plans 10-01 through 10-04 implement all three APL requirements:
    - APL-01: iris variant dispatch via applied_variant.json (no longer inert)
    - APL-02: CLAM variant dispatch via variant_dispatch.py + _make_clam_args seam
    - APL-03: loud-fail for loop-opening LossVariant variants at apply time
    - A1 fix: applied_variant.json overlay mechanism ensures variant selection reaches worktree
  </what-built>
  <how-to-verify>
    Run the full Phase 10 test suite:
      cd /home/jma/Documents/yinshuol/autoMIL
      uv run pytest tests/ benchmarks/tests/ -v -k "not workstation" 2>&1 | tail -40

    Expected:
    - test_apl01_iris_dispatch.py: 3 PASSED (including test_iris_applied_variant_reaches_worktree_at_runtime)
    - test_apl03_loud_fail.py: 4 PASSED
    - test_variant_dispatch_clam.py: 5 PASSED
    - test_lifecycle_apply.py: 14 PASSED
    - All pre-existing tests: 48+ PASSED, 0 FAILED

    Framework purity check (D-206):
      grep -r "autobench" src/automil/ | grep -v "^Binary"
      → must return zero lines

    A1 closure confirmation:
      grep -n "applied_variant.json" examples/sklearn-iris/train.py
      → shows the dispatch read present

    APL-03 ordering confirmation:
      grep -n "_classify_variant_route\|_atomic_write_text" src/automil/cli/lifecycle/apply.py
      → _classify_variant_route appears BEFORE _atomic_write_text in call order
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| applied_variant.json → iris train.py | variant_name string from JSON; used in Path construction via importlib |
| importlib.exec_module → variant module | Loads variant py file from automil/variants/; variant is committed to git |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08 | Tampering | variant_name path traversal in iris train.py dispatch | mitigate | Path(variant_name).name == variant_name check before constructing variants_dir; raises ValueError if check fails |
| T-10-09 | Tampering | Arbitrary code execution via variant module load in train.py | accept | Variant modules are committed to git (registry-first invariant); same threat model as existing scanner.py. Consumer is the operator's own code. |
| T-10-10 | Information Disclosure | AUTOMIL_VARIANT_MODEL env var leaks variant name to subprocess | accept | Variant name is not a secret; it is metadata about the experiment configuration |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
uv run pytest tests/test_apl01_iris_dispatch.py -v
  → 3 PASSED (baseline + dispatch + A1 runtime test)
uv run pytest tests/ benchmarks/tests/ -v -k "not workstation"
  → all passing; no regressions in the 48-test pre-existing suite
grep -r "autobench" src/automil/
  → zero matches
grep -n "applied_variant.json" examples/sklearn-iris/train.py
  → dispatch read present
grep -n "_classify_variant_route" src/automil/cli/lifecycle/apply.py
  → present and before _atomic_write_text (APL-03 ordering)
</verification>

<success_criteria>
1. APL-01 (D-01/D-02): iris train.py reads applied_variant.json and dispatches to make_classifier — 3 APL-01 tests GREEN, including the A1 runtime test.
2. APL-02 (D-03): apply_model_variant_to_exp_cfg patches ModelConfig; run_experiment.py calls it — 5 APL-02 tests GREEN.
3. APL-03 (D-05): LossVariant raises ClickException before config mutation — 4 APL-03 tests GREEN.
4. A1 resolved definitively: variant reaches worktree train.py via applied_variant.json overlay (not just config.yaml). Proven by test_iris_applied_variant_reaches_worktree_at_runtime.
5. Consumer-decoupled contract maintained: zero automil.* imports in iris train.py.
6. Framework purity: zero autobench refs in src/automil/ (D-206).
7. Zero regressions in pre-existing 48-test suite.
</success_criteria>

<output>
Create .planning/phases/10-variant-application-integrity/10-04-SUMMARY.md when done.
</output>
