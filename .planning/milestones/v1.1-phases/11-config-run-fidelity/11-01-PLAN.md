---
phase: 11-config-run-fidelity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_cfg_run_fidelity.py
  - benchmarks/tests/test_run_experiment_config.py
autonomous: true
requirements:
  - CFG-01
  - CFG-02
  - CFG-03
must_haves:
  truths:
    - "tests/test_cfg_run_fidelity.py exists with RED tests for CFG-02 timeout omit/honor, --max-time interaction (D-03), and CFG-03 override write + daemon append"
    - "benchmarks/tests/test_run_experiment_config.py exists with RED tests for CFG-01 None-default honor and explicit-flag override"
    - "Both test files are importable (no syntax errors) and all new tests fail RED for the right reason (assertion or NotImplementedError)"
    - "No existing tests regress — both suites stay green except the new stubs"
  artifacts:
    - path: "tests/test_cfg_run_fidelity.py"
      provides: "CFG-02 + CFG-03 RED test stubs"
    - path: "benchmarks/tests/test_run_experiment_config.py"
      provides: "CFG-01 RED test stubs"
  key_links:
    - from: "tests/test_cfg_run_fidelity.py"
      to: "src/automil/cli/submit.py"
      via: "Click test runner invoking submit"
      pattern: "CliRunner.*submit"
    - from: "benchmarks/tests/test_run_experiment_config.py"
      to: "benchmarks/scripts/run_experiment.py"
      via: "subprocess or argparse parse_args import"
      pattern: "parse_args|run_experiment"
---

<objective>
Wave 0: Create RED test stubs for all CFG behaviors before any production code changes.

Purpose: Nyquist compliance — tests must exist and fail for the right reason before fixes land. Verifies the regression-guard for D-03 (the --max-time sentinel change that is the highest-risk edit in this phase) is pre-registered before submit.py is touched.

Output:
  - tests/test_cfg_run_fidelity.py — CFG-02 (timeout_min omit, honor, max-time interaction D-03) + CFG-03 (override write, daemon append)
  - benchmarks/tests/test_run_experiment_config.py — CFG-01 (None-default honor, explicit-flag override)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-config-run-fidelity/11-CONTEXT.md
@.planning/phases/11-config-run-fidelity/11-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/test_cfg_run_fidelity.py (CFG-02 + CFG-03 RED stubs)</name>
  <files>tests/test_cfg_run_fidelity.py</files>
  <read_first>
    - src/automil/cli/submit.py:29 — current `--timeout default=150` (the value being changed to None by D-02)
    - src/automil/cli/submit.py:60 — current `timeout != 150` sentinel (being changed to `timeout is not None` by D-03)
    - src/automil/cli/submit.py:441 — `"timeout_min": timeout` always written (behavior CFG-02 changes)
    - src/automil/backends/_orchestrator_daemon.py:899-903 — `shlex.split(self.run_command)` launch block (CFG-03 append point)
    - tests/test_cli.py — pattern for CliRunner + automil fixture
    - tests/conftest.py — shared fixtures
  </read_first>
  <action>
Create `tests/test_cfg_run_fidelity.py` with these test functions. All tests must fail RED until the production fixes in Plan 11-03 land.

Tests to write:

**CFG-02: timeout_min omit when --timeout not supplied**
- `test_submit_without_timeout_omits_timeout_min`: invoke `automil submit` without `--timeout`; read the written queue spec JSON; assert `"timeout_min"` is NOT a key in the spec. This fails RED today because submit.py L441 always writes `"timeout_min": timeout` (with 150 as the default).

**CFG-02: timeout_min honored when --timeout supplied**
- `test_submit_with_explicit_timeout_writes_timeout_min`: invoke `automil submit --timeout 90`; assert spec `"timeout_min" == 90`. This should pass RED (it will pass after the fix too — write it now for regression coverage).

**CFG-02 / D-03 regression guard: --max-time wins when both flags given**
- `test_max_time_wins_over_explicit_timeout`: invoke `automil submit --max-time 120 --timeout 99`; assert spec `"timeout_min" == 2` (120 seconds ceil-div to 2 minutes). This is the regression guard for D-03: after changing `timeout != 150` to `timeout is not None`, this combination must still route to the --max-time value. Fails RED today because the sentinel check uses `!= 150` which happens to also catch `99 != 150` — but write it explicitly so we have a named regression test.

**CFG-03: --override writes into queue spec**
- `test_submit_override_written_to_spec`: invoke `automil submit --override "--seed 42 --lr 1e-4"`; read the written queue spec; assert the spec contains the override string (exact field name at implementer's discretion per Claude's Discretion in D-04, e.g. `"run_command_override"` or `"override_args"`). Fails RED today — the `--override` option does not exist yet.

**CFG-03: daemon appends override args after base run.command**
- `test_daemon_appends_override_to_run_command`: unit test the daemon's job-launch logic. Construct a minimal spec dict containing the override field and a `run_command` config value; call the internal launch preparation (or mock subprocess.Popen and call `_launch_job`-equivalent); assert the final `cmd` list equals `shlex.split(base_command) + shlex.split(override_string)`. Confirm no shell=True and no string concatenation. Fails RED today — override field does not exist in daemon.

Use `click.testing.CliRunner` for submit invocations. For tests requiring a minimal project fixture (automil dir, config.yaml, queue dir, graph.json), adapt the fixture pattern from `tests/conftest.py` or `tests/test_cli.py`. Do not call real git; stub or monkeypatch git subprocess calls as the existing test suite does.

Mark test functions with `pytest.mark.unit` if that marker is used in the suite; otherwise leave unmarked.
  </action>
  <verify>
    <automated>cd /home/jma/Documents/yinshuol/autoMIL && uv run pytest tests/test_cfg_run_fidelity.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    File exists; all 5 test functions are importable; tests that depend on not-yet-implemented behavior (test_submit_without_timeout_omits_timeout_min, test_submit_override_written_to_spec, test_daemon_appends_override_to_run_command) fail RED with AssertionError or similar (not ImportError or SyntaxError); existing suite stays green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create benchmarks/tests/test_run_experiment_config.py (CFG-01 RED stubs)</name>
  <files>benchmarks/tests/test_run_experiment_config.py</files>
  <read_first>
    - benchmarks/scripts/run_experiment.py:51-71 — `parse_args()` with hard-coded defaults (L63-68: --max_epochs=200, --lr=1e-4, --seed=42, --n_folds=5, --patience=20, --stop_epoch=50)
    - benchmarks/scripts/run_experiment.py:155-161 — `TrainConfig(...)` construction passing all arg values unconditionally
    - benchmarks/src/autobench/pipeline/config.py:67-76 — `TrainConfig` dataclass with its defaults: lr=2e-4, seed=42, max_epochs=200, patience=20, stop_epoch=50; ExperimentConfig.n_folds=10
    - benchmarks/tests/test_benchmark_config.py — existing test patterns for the autobench package
  </read_first>
  <action>
Create `benchmarks/tests/test_run_experiment_config.py` with these test functions. All tests must fail RED until the production fix in Plan 11-02 lands.

The key discrepancy to test: `TrainConfig.lr` defaults to `2e-4` in the dataclass but `run_experiment.py parse_args()` defaults `--lr` to `1e-4`. When --lr is not passed on the CLI, the current code passes `args.lr = 1e-4` into `TrainConfig`, overriding the dataclass default of `2e-4`. The fix changes `--lr` default to `None` and only passes it when not None.

Also test `n_folds`: `ExperimentConfig.n_folds` defaults to `10` in the dataclass but `parse_args()` defaults `--n_folds` to `5`. Same masking pattern.

Tests to write (import `parse_args` and the `TrainConfig`/`ExperimentConfig` construction logic directly — no subprocess needed; import the function from the script):

**CFG-01: None default honors dataclass lr**
- `test_no_lr_flag_uses_trainconfig_default`: simulate running parse_args with `["--dataset", "x", "--task", "x", "--encoder", "x", "--model", "x", "--framework", "clam"]` (no --lr); confirm that under the fixed code the TrainConfig would receive `lr=None` or, equivalently, that the final TrainConfig.lr == 2e-4 (the dataclass default). Test the parse_args return value: `assert args.lr is None`. Fails RED today because `args.lr == 1e-4` (the hard-coded argparse default).

**CFG-01: None default honors dataclass n_folds**
- `test_no_n_folds_flag_uses_experimentconfig_default`: same pattern for n_folds; assert `args.n_folds is None`. Fails RED today (`args.n_folds == 5`).

**CFG-01: explicit flag still overrides**
- `test_explicit_lr_flag_is_honored`: simulate parse_args with `--lr 5e-4`; assert `args.lr == 5e-4`. This passes RED today and after the fix — write for regression coverage.

**CFG-01: explicit n_folds flag still overrides**
- `test_explicit_n_folds_flag_is_honored`: parse_args with `--n_folds 3`; assert `args.n_folds == 3`. Same — regression coverage.

Import `parse_args` by importing the module: `import importlib.util; spec = importlib.util.spec_from_file_location(...)` or by adjusting sys.path to include `benchmarks/scripts/`. Do not actually run the experiment (no real data needed — only test the argparse layer).

Do not mark with `workstation` marker — these are pure unit tests of argparse behavior, no data or GPU required.
  </action>
  <verify>
    <automated>cd /home/jma/Documents/yinshuol/autoMIL && uv run pytest benchmarks/tests/test_run_experiment_config.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    File exists; test_no_lr_flag_uses_trainconfig_default and test_no_n_folds_flag_uses_experimentconfig_default fail RED (AssertionError: 1e-4 != None and 5 != None respectively); test_explicit_lr_flag_is_honored and test_explicit_n_folds_flag_is_honored pass GREEN; no existing benchmark test regresses.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → production imports | Test stubs import from src/automil and benchmarks/scripts; no untrusted data crosses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-SC | Tampering | No new npm/pip/cargo installs in Wave 0 | accept | No package installs; pure Python test file creation |
</threat_model>

<verification>
After both tasks complete:

```bash
# Framework suite — run separately from benchmark suite (rootdir collision)
cd /home/jma/Documents/yinshuol/autoMIL && uv run pytest tests/ -q 2>&1 | tail -10

# Benchmark suite — separate invocation
cd /home/jma/Documents/yinshuol/autoMIL && uv run pytest benchmarks/tests/ -q 2>&1 | tail -10
```

Expected: existing tests all pass; new stubs show the expected RED failures.
</verification>

<success_criteria>
- tests/test_cfg_run_fidelity.py exists with 5 tests (3 RED, 2 GREEN or all RED acceptable since regression tests may be passing)
- benchmarks/tests/test_run_experiment_config.py exists with 4 tests (2 RED, 2 GREEN)
- Zero existing tests regress in either suite
- Both files are importable (no SyntaxError, no ImportError on collection)
</success_criteria>

<output>
Create `.planning/phases/11-config-run-fidelity/11-01-SUMMARY.md` when done.
</output>
